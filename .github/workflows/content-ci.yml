name: Content CI

on:
  pull_request:
  push:
    branches: [ main, master ]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Validate topics contract
        run: python3 interview/Scripts/check_topics_contract.py

      - name: Validate topics JSON parse
        run: python3 -c "import json;json.load(open('interview/Config/topics.json'))"

      - name: Validate paths and topic meta
        run: python3 interview/Scripts/check_paths_and_meta.py

      - name: Validate OTM sample models
        run: python3 interview/Scripts/check_otm_models.py

      - name: Build search index
        run: python3 interview/Scripts/build_search_index.py

      - name: Generate sitemap
        run: python3 interview/Scripts/generate_sitemap.py

      - name: Generate Interview Preparation index
        run: python3 interview/Scripts/generate_interview_preparation.py

      - name: Check markdown links
        uses: lycheeverse/lychee-action@v1
        with:
          fail: true
          # Config: .lychee.toml (retries + accept transient 429/502/503/504).
          args: --no-progress --verbose --config .lychee.toml "./**/*.md"

