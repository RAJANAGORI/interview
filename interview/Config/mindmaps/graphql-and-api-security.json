{
  "id": "n59c6c81d9181a51",
  "title": "GraphQL and API Security",
  "markdown": "GraphQL is a query language and runtime for APIs developed internally at Facebook in 2012 and open-sourced in 2015. It gives clients the power to request exactly the data they need through a single endpoint, replacing the resource-oriented URL structure of REST with a schema-driven, type-safe contract between client and server. That flexibility accelerates product development but introduces security challenges that are fundamentally different from traditional REST APIs.\n\nIn a REST API, the attack surface is defined by the set of routes and HTTP methods. Securing REST means locking down each endpoint with middleware, validating inputs at known boundaries, and rate-limiting per URL. In GraphQL, the attack surface is defined by the **schema** - a graph of interconnected types, fields, and resolvers - and the nearly unbounded set of queries a client can construct against it. A single GraphQL endpoint can service queries of wildly different cost, depth, and authorization requirements, all via `POST /graphql`.\n\nThis guide covers the security properties unique to GraphQL, the failure modes that arise from its flexibility, and the operational controls that make it safe to run in production.",
  "notes": "GraphQL is a query language and runtime for APIs developed internally at Facebook in 2012 and open-sourced in 2015. It gives clients the power to request exactly the data they need through a single endpoint, replacing the resource-oriented URL structure of REST with a schema-driven, type-safe contract between client and server. That flexibility accelerates product development but introduces security challenges that are fundamentally different from traditional REST APIs.\n\nIn a REST API, the attack surface is defined by the set of routes and HTTP methods. Securing REST means locking down each endpoint with middleware, validating inputs at known boundaries, and rate-limiting per URL. In GraphQL, the attack surface is defined by the schema - a graph of interconnected types, fields, and resolvers - and the nearly unbounded set of queries a client can construct against it. A single GraphQL endpoint can service queries of wildly different cost, depth, and authorization requirements, all via POST /graphql.\n\nThis guide covers the security properties unique to GraphQL, the failure modes that arise from its flexibility, and the operational controls that make it safe to run in production.",
  "expanded": true,
  "children": [
    {
      "id": "ncfbbd4571048328",
      "title": "Concept",
      "markdown": "GraphQL is a query language and runtime for APIs developed internally at Facebook in 2012 and open-sourced in 2015. It gives clients the power to request exactly the data they need through a single endpoint, replacing the resource-oriented URL structure of REST with a schema-driven, type-safe contract between client and server. That flexibility accelerates product development but introduces security challenges that are fundamentally different from traditional REST APIs.\n\nIn a REST API, the attack surface is defined by the set of routes and HTTP methods. Securing REST means locking down each endpoint with middleware, validating inputs at known boundaries, and rate-limiting per URL. In GraphQL, the attack surface is defined by the **schema** - a graph of interconnected types, fields, and resolvers - and the nearly unbounded set of queries a client can construct against it. A single GraphQL endpoint can service queries of wildly different cost, depth, and authorization requirements, all via `POST /graphql`.\n\nThis guide covers the security properties unique to GraphQL, the failure modes that arise from its flexibility, and the operational controls that make it safe to run in production.",
      "notes": "GraphQL is a query language and runtime for APIs developed internally at Facebook in 2012 and open-sourced in 2015. It gives clients the power to request exactly the data they need through a single endpoint, replacing the resource-oriented URL structure of REST with a schema-driven, type-safe contract between client and server. That flexibility accelerates product development but introduces security challenges that are fundamentally different from traditional REST APIs.\n\nIn a REST API, the attack surface is defined by the set of routes and HTTP methods. Securing REST means locking down each endpoint with middleware, validating inputs at known boundaries, and rate-limiting per URL. In GraphQL, the attack surface is defined by the schema - a graph of interconnected types, fields, and resolvers - and the nearly unbounded set of queries a client can construct against it. A single GraphQL endpoint can service queries of wildly different cost, depth, and authorization requirements, all via POST /graphql.\n\nThis guide covers the security properties unique to GraphQL, the failure modes that arise from its flexibility, and the operational controls that make it safe to run in production.",
      "kind": "topic",
      "level": 2,
      "children": [],
      "blurb": "GraphQL is a query language and runtime for APIs developed internally at...",
      "expanded": false
    },
    {
      "id": "ncf3f75d0dbd1daa",
      "title": "GraphQL Fundamentals Refresher",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "nd66481172ced524",
          "title": "Schema and Type System",
          "markdown": "A GraphQL schema is the contract between client and server. It defines every type, field, relationship, and operation the API supports using the Schema Definition Language (SDL):\n\n```graphql\ntype User {\n  id: ID!\n  email: String!\n  name: String!\n  role: Role!\n  orders(first: Int, after: String): OrderConnection!\n  organization: Organization!\n}\n\ntype Organization {\n  id: ID!\n  name: String!\n  members: [User!]!\n  billingInfo: BillingInfo\n}\n\nenum Role {\n  ADMIN\n  MEMBER\n  VIEWER\n}\n\ntype Query {\n  me: User!\n  user(id: ID!): User\n  users(filter: UserFilter): [User!]!\n}\n\ntype Mutation {\n  updateUser(id: ID!, input: UpdateUserInput!): User!\n  deleteUser(id: ID!): Boolean!\n}\n\ntype Subscription {\n  orderStatusChanged(orderId: ID!): Order!\n}\n```\n\nThe schema is simultaneously the API documentation, the type-checking system, and the attack surface map. Every type and field an attacker can reach is defined here, and the relationships between types determine the traversal paths available for nested attacks.",
          "notes": "A GraphQL schema is the contract between client and server. It defines every type, field, relationship, and operation the API supports using the Schema Definition Language (SDL):\n\ntype User {\n  id: ID!\n  email: String!\n  name: String!\n  role: Role!\n  orders(first: Int, after: String): OrderConnection!\n  organization: Organization!\n}\n\ntype Organization {\n  id: ID!\n  name: String!\n  members: [User!]!\n  billingInfo: BillingInfo\n}\n\nenum Role {\n  ADMIN\n  MEMBER\n  VIEWER\n}\n\ntype Query {\n  me: User!\n  user(id: ID!): User\n  users(filter: UserFilter): [User!]!\n}\n\ntype Mutation {\n  updateUser(id: ID!, input: UpdateUserInput!): User!\n  deleteUser(id: ID!): Boolean!\n}\n\ntype Subscription {\n  orderStatusChanged(orderId: ID!): Order!\n}\n\nThe schema is simultaneously the API documentation, the type-checking system, and the attack surface map. Every type and field an attacker can reach is defined here, and the relationships between types determine the traversal paths available for nested attacks.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "A GraphQL schema is the contract between client and server.",
          "expanded": false
        },
        {
          "id": "nad575485c9f96a2",
          "title": "Resolvers",
          "markdown": "Each field in the schema is backed by a **resolver** - a function that fetches or computes the field's value. Resolvers are where the actual data access, business logic, and authorization decisions happen:\n\n```javascript\nconst resolvers = {\n  Query: {\n    user: (parent, { id }, context) => {\n      return context.dataSources.userAPI.getUser(id);\n    },\n  },\n  User: {\n    orders: (user, { first, after }, context) => {\n      return context.dataSources.orderAPI.getOrdersForUser(user.id, { first, after });\n    },\n    organization: (user, args, context) => {\n      return context.dataSources.orgAPI.getOrg(user.organizationId);\n    },\n  },\n};\n```\n\nEach resolver call is a potential database query, service call, or compute operation. The client controls which resolvers fire by choosing which fields to include in the query. This is the root of GraphQL's performance and security complexity: the server does not know the cost of a query until it parses and analyzes it.",
          "notes": "Each field in the schema is backed by a resolver - a function that fetches or computes the field's value. Resolvers are where the actual data access, business logic, and authorization decisions happen:\n\nconst resolvers = {\n  Query: {\n    user: (parent, { id }, context) => {\n      return context.dataSources.userAPI.getUser(id);\n    },\n  },\n  User: {\n    orders: (user, { first, after }, context) => {\n      return context.dataSources.orderAPI.getOrdersForUser(user.id, { first, after });\n    },\n    organization: (user, args, context) => {\n      return context.dataSources.orgAPI.getOrg(user.organizationId);\n    },\n  },\n};\n\nEach resolver call is a potential database query, service call, or compute operation. The client controls which resolvers fire by choosing which fields to include in the query. This is the root of GraphQL's performance and security complexity: the server does not know the cost of a query until it parses and analyzes it.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Each field in the schema is backed by a resolver - a function that fetches...",
          "expanded": false
        },
        {
          "id": "nf1d80f831378f8c",
          "title": "Queries, Mutations, and Subscriptions",
          "markdown": "Queries are read operations.",
          "notes": "Queries are read operations.",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "n7b1c3e833eea8e8",
              "title": "Queries are read operations. They can nest arbitrarily deep following the type graph.",
              "markdown": "- Queries are read operations. They can nest arbitrarily deep following the type graph.",
              "notes": "Queries are read operations. They can nest arbitrarily deep following the type graph.",
              "blurb": "Queries are read operations.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n042fb1ea6f9651b",
              "title": "Mutations are write operations. They execute sequentially (unlike queries, which can run",
              "markdown": "- Mutations are write operations. They execute sequentially (unlike queries, which can run",
              "notes": "Mutations are write operations. They execute sequentially (unlike queries, which can run",
              "blurb": "Mutations are write operations.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "ne64e45ec0c5fcdb",
              "title": "Subscriptions open a persistent connection (typically WebSocket) for real-time data. The",
              "markdown": "- Subscriptions open a persistent connection (typically WebSocket) for real-time data. The",
              "notes": "Subscriptions open a persistent connection (typically WebSocket) for real-time data. The",
              "blurb": "Subscriptions open a persistent connection (typically WebSocket) for...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "Queries are read operations.",
          "expanded": false
        },
        {
          "id": "n2d0914ca36fcb9e",
          "title": "Execution Model",
          "markdown": "When a GraphQL server receives an operation, it:",
          "notes": "When a GraphQL server receives an operation, it:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "n88f0351d082857e",
              "title": "Parses the query string into an AST (Abstract Syntax Tree).",
              "markdown": "- Parses the query string into an AST (Abstract Syntax Tree).",
              "notes": "Parses the query string into an AST (Abstract Syntax Tree).",
              "blurb": "Parses the query string into an AST (Abstract Syntax Tree).",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n598514c9c45c39b",
              "title": "Validates the AST against the schema (type checking, field existence, argument types).",
              "markdown": "- Validates the AST against the schema (type checking, field existence, argument types).",
              "notes": "Validates the AST against the schema (type checking, field existence, argument types).",
              "blurb": "Validates the AST against the schema (type checking, field existence,...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n305aeef09561bb6",
              "title": "Executes the operation by walking the AST and calling resolvers breadth-first, level by",
              "markdown": "- Executes the operation by walking the AST and calling resolvers breadth-first, level by",
              "notes": "Executes the operation by walking the AST and calling resolvers breadth-first, level by",
              "blurb": "Executes the operation by walking the AST and calling resolvers...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n74d99d239bdb5d8",
              "title": "Serializes the result into the response shape matching the query.",
              "markdown": "- Serializes the result into the response shape matching the query.",
              "notes": "Serializes the result into the response shape matching the query.",
              "blurb": "Serializes the result into the response shape matching the query.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "When a GraphQL server receives an operation, it:",
          "expanded": false
        }
      ],
      "blurb": "Includes Schema and Type System",
      "expanded": true
    },
    {
      "id": "n93490cad08ff933",
      "title": "Introspection Attacks",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n4c460fd0af21150",
          "title": "What Introspection Exposes",
          "markdown": "GraphQL introspection is a built-in capability defined in the specification that allows clients to query the schema itself. The `__schema` and `__type` meta-fields return the complete type system:\n\n```graphql\nquery IntrospectionQuery {\n  __schema {\n    types {\n      name\n      kind\n      fields {\n        name\n        type {\n          name\n          kind\n          ofType {\n            name\n          }\n        }\n        args {\n          name\n          type { name }\n        }\n      }\n    }\n    mutationType {\n      fields {\n        name\n        args { name type { name } }\n      }\n    }\n  }\n}\n```\n\nThis query returns every type, field, argument, enum value, and relationship in the API. For an attacker, it is the equivalent of obtaining the complete API documentation, database schema hints, and relationship map in a single request. They can identify sensitive fields (`ssn`, `billingInfo`, `internalNotes`), discover mutations (`deleteUser`, `promoteToAdmin`, `transferFunds`), and map traversal paths for IDOR attacks.",
          "notes": "GraphQL introspection is a built-in capability defined in the specification that allows clients to query the schema itself. The schema and type meta-fields return the complete type system:\n\nquery IntrospectionQuery {\n  schema {\n    types {\n      name\n      kind\n      fields {\n        name\n        type {\n          name\n          kind\n          ofType {\n            name\n          }\n        }\n        args {\n          name\n          type { name }\n        }\n      }\n    }\n    mutationType {\n      fields {\n        name\n        args { name type { name } }\n      }\n    }\n  }\n}\n\nThis query returns every type, field, argument, enum value, and relationship in the API. For an attacker, it is the equivalent of obtaining the complete API documentation, database schema hints, and relationship map in a single request. They can identify sensitive fields (ssn, billingInfo, internalNotes), discover mutations (deleteUser, promoteToAdmin, transferFunds), and map traversal paths for IDOR attacks.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "GraphQL introspection is a built-in capability defined in the specification...",
          "expanded": false
        },
        {
          "id": "ne8e59aee06d4437",
          "title": "Attack Scenarios",
          "markdown": "**Schema reconnaissance:** An attacker runs the full introspection query to map the API surface before probing for authorization gaps. Tools like GraphQL Voyager visualize the schema as an interactive graph, making it trivial to identify high-value targets.\n\n**Discovering hidden functionality:** Even when a frontend client only uses a subset of the schema, introspection reveals all available operations. Internal admin mutations, deprecated but still-active fields, and experimental features are all visible.\n\n**Identifying input types for injection:** Introspection reveals argument types and input objects, helping an attacker craft malformed inputs targeting specific resolvers.",
          "notes": "Schema reconnaissance: An attacker runs the full introspection query to map the API surface before probing for authorization gaps. Tools like GraphQL Voyager visualize the schema as an interactive graph, making it trivial to identify high-value targets.\n\nDiscovering hidden functionality: Even when a frontend client only uses a subset of the schema, introspection reveals all available operations. Internal admin mutations, deprecated but still-active fields, and experimental features are all visible.\n\nIdentifying input types for injection: Introspection reveals argument types and input objects, helping an attacker craft malformed inputs targeting specific resolvers.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Schema reconnaissance: An attacker runs the full introspection query to map...",
          "expanded": false
        },
        {
          "id": "n5d9f12439d13c81",
          "title": "Mitigations",
          "markdown": "**Disable introspection in production:** Most GraphQL servers support this as a configuration option. Apollo Server disables it by default in production mode. For custom servers, reject queries containing `__schema` or `__type` at the validation layer.\n\n**Authenticated introspection:** Instead of fully disabling, restrict introspection to authenticated users with specific roles. This preserves developer tooling for internal users while blocking unauthenticated reconnaissance.\n\n**Schema registry / CI publishing:** Publish the schema through CI/CD to internal documentation tools, developer portals, or schema registries (Apollo Studio, GraphQL Hive) instead of relying on runtime introspection.\n\n**Field suggestion suppression:** Even with introspection disabled, GraphQL servers often return error messages like `Did you mean \"email\"?` when an invalid field is queried. These suggestions leak valid field names. Configure the server to suppress field suggestions in production, or replace them with generic error messages.\n\n```javascript\nconst server = new ApolloServer({\n  introspection: false,\n  formatError: (error) => {\n    if (error.message.startsWith('Cannot query field')) {\n      return new Error('Invalid field');\n    }\n    return error;\n  },\n});\n```",
          "notes": "Disable introspection in production: Most GraphQL servers support this as a configuration option. Apollo Server disables it by default in production mode. For custom servers, reject queries containing schema or type at the validation layer.\n\nAuthenticated introspection: Instead of fully disabling, restrict introspection to authenticated users with specific roles. This preserves developer tooling for internal users while blocking unauthenticated reconnaissance.\n\nSchema registry / CI publishing: Publish the schema through CI/CD to internal documentation tools, developer portals, or schema registries (Apollo Studio, GraphQL Hive) instead of relying on runtime introspection.\n\nField suggestion suppression: Even with introspection disabled, GraphQL servers often return error messages like Did you mean \"email\"? when an invalid field is queried. These suggestions leak valid field names. Configure the server to suppress field suggestions in production, or replace them with generic error messages.\n\nconst server = new ApolloServer({\n  introspection: false,\n  formatError: (error) => {\n    if (error.message.startsWith('Cannot query field')) {\n      return new Error('Invalid field');\n    }\n    return error;\n  },\n});",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Disable introspection in production: Most GraphQL servers support this as a...",
          "expanded": false
        }
      ],
      "blurb": "Includes What Introspection Exposes",
      "expanded": true
    },
    {
      "id": "nd14b6b0bae56ece",
      "title": "Query Complexity and Denial of Service",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "ne718599b4947a16",
          "title": "The Depth Problem",
          "markdown": "GraphQL allows clients to construct queries of arbitrary depth by following type relationships. A schema with circular references (which is common - users have organizations, organizations have members who are users) enables unbounded nesting:\n\n```graphql\nquery DeepNesting {\n  user(id: \"1\") {\n    organization {\n      members {\n        organization {\n          members {\n            organization {\n              members {\n                # ... continue indefinitely\n                name\n              }\n            }\n          }\n        }\n      }\n    }\n  }\n}\n```\n\nEach level of nesting triggers a new set of resolver calls and database queries. A depth of 10 levels on a type graph with fan-out could trigger millions of resolver calls from a single HTTP request.",
          "notes": "GraphQL allows clients to construct queries of arbitrary depth by following type relationships. A schema with circular references (which is common - users have organizations, organizations have members who are users) enables unbounded nesting:\n\nquery DeepNesting {\n  user(id: \"1\") {\n    organization {\n      members {\n        organization {\n          members {\n            organization {\n              members {\n                # ... continue indefinitely\n                name\n              }\n            }\n          }\n        }\n      }\n    }\n  }\n}\n\nEach level of nesting triggers a new set of resolver calls and database queries. A depth of 10 levels on a type graph with fan-out could trigger millions of resolver calls from a single HTTP request.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "GraphQL allows clients to construct queries of arbitrary depth by following...",
          "expanded": false
        },
        {
          "id": "nbffb64d1a44a542",
          "title": "Depth Limiting",
          "markdown": "The simplest defense is a hard limit on query depth. Libraries like `graphql-depth-limit` reject queries exceeding a configured maximum before execution:\n\n```javascript\nimport depthLimit from 'graphql-depth-limit';\n\nconst server = new ApolloServer({\n  validationRules: [depthLimit(10)],\n});\n```\n\nDepth limiting is easy to implement but coarse. A query at depth 5 selecting 100 fields per level is far more expensive than a query at depth 15 selecting one field per level. Depth limits should be combined with cost analysis.",
          "notes": "The simplest defense is a hard limit on query depth. Libraries like graphql-depth-limit reject queries exceeding a configured maximum before execution:\n\nimport depthLimit from 'graphql-depth-limit';\n\nconst server = new ApolloServer({\n  validationRules: [depthLimit(10)],\n});\n\nDepth limiting is easy to implement but coarse. A query at depth 5 selecting 100 fields per level is far more expensive than a query at depth 15 selecting one field per level. Depth limits should be combined with cost analysis.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "The simplest defense is a hard limit on query depth.",
          "expanded": false
        },
        {
          "id": "nf5d1909cfa71808",
          "title": "Cost Analysis and Complexity Scoring",
          "markdown": "Cost analysis assigns a numeric weight to each field and computes the total cost of a query before execution. If the cost exceeds a threshold, the query is rejected.\n\n**Field-level cost annotations:**\n\n```javascript\nconst typeDefs = gql`\n  type Query {\n    users(first: Int): [User!]! @cost(complexity: 10, multipliers: [\"first\"])\n    user(id: ID!): User @cost(complexity: 1)\n  }\n\n  type User {\n    id: ID! @cost(complexity: 0)\n    email: String! @cost(complexity: 0)\n    orders(first: Int): [Order!]! @cost(complexity: 5, multipliers: [\"first\"])\n    recommendations: [Product!]! @cost(complexity: 50)\n  }\n`;\n```\n\n**Cost calculation algorithm:**\n\nThe total cost is computed recursively. For a field with a list multiplier, the cost of its children is multiplied by the expected list size:\n\n```\ncost(field) = fieldCost + (multiplier \u00d7 sum(cost(child) for each child field))\n```\n\nFor example, `users(first: 100) { orders(first: 50) { items { name } } }` would compute as:",
          "notes": "Cost analysis assigns a numeric weight to each field and computes the total cost of a query before execution. If the cost exceeds a threshold, the query is rejected.\n\nField-level cost annotations:\n\nconst typeDefs = gql\n  type Query {\n    users(first: Int): [User!]! @cost(complexity: 10, multipliers: [\"first\"])\n    user(id: ID!): User @cost(complexity: 1)\n  }\n\n  type User {\n    id: ID! @cost(complexity: 0)\n    email: String! @cost(complexity: 0)\n    orders(first: Int): [Order!]! @cost(complexity: 5, multipliers: [\"first\"])\n    recommendations: [Product!]! @cost(complexity: 50)\n  }\n;\n\nCost calculation algorithm:\n\nThe total cost is computed recursively. For a field with a list multiplier, the cost of its children is multiplied by the expected list size:\n\ncost(field) = fieldCost + (multiplier \u00d7 sum(cost(child) for each child field))\n\nFor example, users(first: 100) { orders(first: 50) { items { name } } } would compute as:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "n23814152a3fbe30",
              "title": "items.name: 0",
              "markdown": "- items.name: 0",
              "notes": "items.name: 0",
              "blurb": "",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nd67d6b1d0dc20e8",
              "title": "items: 0 + (1 \u00d7 0) = 0",
              "markdown": "- items: 0 + (1 \u00d7 0) = 0",
              "notes": "items: 0 + (1 \u00d7 0) = 0",
              "blurb": "items: 0 + (1 \u00d7 0) = 0",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n7ffefd12cf2d7f0",
              "title": "orders: 5 + (50 \u00d7 0) = 5",
              "markdown": "- orders: 5 + (50 \u00d7 0) = 5",
              "notes": "orders: 5 + (50 \u00d7 0) = 5",
              "blurb": "orders: 5 + (50 \u00d7 0) = 5",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nc5d63aecc4433b8",
              "title": "users: 10 + (100 \u00d7 5) = 510",
              "markdown": "- users: 10 + (100 \u00d7 5) = 510",
              "notes": "users: 10 + (100 \u00d7 5) = 510",
              "blurb": "users: 10 + (100 \u00d7 5) = 510",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "Cost analysis assigns a numeric weight to each field and computes the total...",
          "expanded": false
        },
        {
          "id": "nb29aaea56e7258b",
          "title": "Timeout and Resource Guards",
          "markdown": "Cost analysis operates before execution. Runtime guards protect against queries that pass static analysis but still consume excessive resources:",
          "notes": "Cost analysis operates before execution. Runtime guards protect against queries that pass static analysis but still consume excessive resources:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "n6e682705b9d065c",
              "title": "Resolver-level timeouts: Kill individual resolver calls that exceed a duration threshold",
              "markdown": "- Resolver-level timeouts: Kill individual resolver calls that exceed a duration threshold",
              "notes": "Resolver-level timeouts: Kill individual resolver calls that exceed a duration threshold",
              "blurb": "Resolver-level timeouts: Kill individual resolver calls that exceed a...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nade057426ae255b",
              "title": "Operation-level timeouts: Kill the entire operation if total execution time exceeds a li",
              "markdown": "- Operation-level timeouts: Kill the entire operation if total execution time exceeds a li",
              "notes": "Operation-level timeouts: Kill the entire operation if total execution time exceeds a li",
              "blurb": "Operation-level timeouts: Kill the entire operation if total execution time...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n1567ed50acb43cf",
              "title": "Connection pool limits: Bound the number of concurrent database connections a single ope",
              "markdown": "- Connection pool limits: Bound the number of concurrent database connections a single ope",
              "notes": "Connection pool limits: Bound the number of concurrent database connections a single ope",
              "blurb": "Connection pool limits: Bound the number of concurrent database connections...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n7efdbaf84160628",
              "title": "DataLoader batching: Use DataLoader to batch and deduplicate resolver calls within a sin",
              "markdown": "- DataLoader batching: Use DataLoader to batch and deduplicate resolver calls within a sin",
              "notes": "DataLoader batching: Use DataLoader to batch and deduplicate resolver calls within a sin",
              "blurb": "DataLoader batching: Use DataLoader to batch and deduplicate resolver calls...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "Cost analysis operates before execution.",
          "expanded": false
        }
      ],
      "blurb": "Includes The Depth Problem",
      "expanded": true
    },
    {
      "id": "n580c4ff48c808cc",
      "title": "Batching and Alias Abuse",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n880974a49a27eb2",
          "title": "How Batching Works",
          "markdown": "GraphQL servers commonly accept an array of operations in a single HTTP request:\n\n```json\n[\n  { \"query\": \"mutation { login(user: \\\"admin\\\", pass: \\\"password1\\\") { token } }\" },\n  { \"query\": \"mutation { login(user: \\\"admin\\\", pass: \\\"password2\\\") { token } }\" },\n  { \"query\": \"mutation { login(user: \\\"admin\\\", pass: \\\"password3\\\") { token } }\" }\n]\n```\n\nAn attacker can pack hundreds or thousands of operations into a single HTTP request, bypassing per-request rate limits while generating massive server-side load. A batch of 1,000 login mutations is one HTTP request but 1,000 authentication attempts.",
          "notes": "GraphQL servers commonly accept an array of operations in a single HTTP request:\n\n[\n  { \"query\": \"mutation { login(user: \\\"admin\\\", pass: \\\"password1\\\") { token } }\" },\n  { \"query\": \"mutation { login(user: \\\"admin\\\", pass: \\\"password2\\\") { token } }\" },\n  { \"query\": \"mutation { login(user: \\\"admin\\\", pass: \\\"password3\\\") { token } }\" }\n]\n\nAn attacker can pack hundreds or thousands of operations into a single HTTP request, bypassing per-request rate limits while generating massive server-side load. A batch of 1,000 login mutations is one HTTP request but 1,000 authentication attempts.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "GraphQL servers commonly accept an array of operations in a single HTTP...",
          "expanded": false
        },
        {
          "id": "na53abe2f69d5836",
          "title": "Alias Abuse",
          "markdown": "Even without array batching, GraphQL aliases allow repeating the same field or operation under different names within a single query:\n\n```graphql\nquery BruteForce {\n  a1: login(user: \"admin\", pass: \"password1\") { token }\n  a2: login(user: \"admin\", pass: \"password2\") { token }\n  a3: login(user: \"admin\", pass: \"password3\") { token }\n  # ... hundreds more aliases\n  a500: login(user: \"admin\", pass: \"password500\") { token }\n}\n```\n\nThis is a single GraphQL operation containing 500 resolver calls. Each alias invokes the `login` resolver independently. Traditional rate limiting sees one HTTP request. Even query complexity analysis may undercount if aliases are not properly accounted for.\n\n**Resource amplification example:**\n\n```graphql\nquery Amplify {\n  a1: expensiveSearch(query: \"a\") { results { details { relatedItems { name } } } }\n  a2: expensiveSearch(query: \"b\") { results { details { relatedItems { name } } } }\n  a3: expensiveSearch(query: \"c\") { results { details { relatedItems { name } } } }\n  # 50 aliases \u00d7 expensive search \u00d7 nested resolution = massive backend load\n}\n```",
          "notes": "Even without array batching, GraphQL aliases allow repeating the same field or operation under different names within a single query:\n\nquery BruteForce {\n  a1: login(user: \"admin\", pass: \"password1\") { token }\n  a2: login(user: \"admin\", pass: \"password2\") { token }\n  a3: login(user: \"admin\", pass: \"password3\") { token }\n  # ... hundreds more aliases\n  a500: login(user: \"admin\", pass: \"password500\") { token }\n}\n\nThis is a single GraphQL operation containing 500 resolver calls. Each alias invokes the login resolver independently. Traditional rate limiting sees one HTTP request. Even query complexity analysis may undercount if aliases are not properly accounted for.\n\nResource amplification example:\n\nquery Amplify {\n  a1: expensiveSearch(query: \"a\") { results { details { relatedItems { name } } } }\n  a2: expensiveSearch(query: \"b\") { results { details { relatedItems { name } } } }\n  a3: expensiveSearch(query: \"c\") { results { details { relatedItems { name } } } }\n  # 50 aliases \u00d7 expensive search \u00d7 nested resolution = massive backend load\n}",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Even without array batching, GraphQL aliases allow repeating the same field...",
          "expanded": false
        },
        {
          "id": "n0bbdb93f252b9be",
          "title": "Mitigations",
          "markdown": "**Limit batch size:** Cap the number of operations per HTTP request (e.g., maximum 5 or 10). Reject requests exceeding the limit.\n\n**Count aliases in cost analysis:** Ensure the complexity scoring system treats each alias as a separate invocation. The cost of `a1: login(...) a2: login(...)` should be 2\u00d7 the cost of a single `login(...)`.\n\n**Operation-aware rate limiting:** Rate limit by operation name, mutation type, or resolved field rather than HTTP request count. If the `login` mutation is called 500 times via aliases, count it as 500 authentication attempts.\n\n**Disable unnecessary batching:** If your clients don't need array batching, disable it entirely. Many servers enable it by default.",
          "notes": "Limit batch size: Cap the number of operations per HTTP request (e.g., maximum 5 or 10). Reject requests exceeding the limit.\n\nCount aliases in cost analysis: Ensure the complexity scoring system treats each alias as a separate invocation. The cost of a1: login(...) a2: login(...) should be 2\u00d7 the cost of a single login(...).\n\nOperation-aware rate limiting: Rate limit by operation name, mutation type, or resolved field rather than HTTP request count. If the login mutation is called 500 times via aliases, count it as 500 authentication attempts.\n\nDisable unnecessary batching: If your clients don't need array batching, disable it entirely. Many servers enable it by default.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Limit batch size: Cap the number of operations per HTTP request (e.g.,...",
          "expanded": false
        }
      ],
      "blurb": "Includes How Batching Works",
      "expanded": false
    },
    {
      "id": "n0492e226be0d7c4",
      "title": "Authorization Patterns",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n686cf3715a6c624",
          "title": "The Core Problem",
          "markdown": "GraphQL has no built-in authorization model. The specification defines type checking and execution semantics but says nothing about who can access what. Authorization is entirely the responsibility of the application layer. This is by design - but it means every GraphQL API must consciously implement authorization, and the graph structure creates authorization challenges that don't exist in REST.\n\nIn REST, authorization is typically enforced at the route/controller level: the `/admin/users` endpoint checks for admin role, and all fields returned by that endpoint inherit the same authorization. In GraphQL, a single query can traverse from a public type to a sensitive one through a chain of relationships:\n\n```graphql\nquery {\n  publicPost(id: \"123\") {        # Public - anyone can access\n    author {                      # User type - some fields restricted\n      email                       # Should be restricted to the user themselves or admins\n      organization {              # Organization type - different authorization domain\n        billingInfo {              # Highly sensitive - only billing admins\n          creditCardLast4\n        }\n      }\n    }\n  }\n}\n```\n\nIf authorization is only checked at the query entry point (`publicPost`), the traversal to `billingInfo` is unauthorized but succeeds because the resolver trusts the parent context.",
          "notes": "GraphQL has no built-in authorization model. The specification defines type checking and execution semantics but says nothing about who can access what. Authorization is entirely the responsibility of the application layer. This is by design - but it means every GraphQL API must consciously implement authorization, and the graph structure creates authorization challenges that don't exist in REST.\n\nIn REST, authorization is typically enforced at the route/controller level: the /admin/users endpoint checks for admin role, and all fields returned by that endpoint inherit the same authorization. In GraphQL, a single query can traverse from a public type to a sensitive one through a chain of relationships:\n\nquery {\n  publicPost(id: \"123\") {        # Public - anyone can access\n    author {                      # User type - some fields restricted\n      email                       # Should be restricted to the user themselves or admins\n      organization {              # Organization type - different authorization domain\n        billingInfo {              # Highly sensitive - only billing admins\n          creditCardLast4\n        }\n      }\n    }\n  }\n}\n\nIf authorization is only checked at the query entry point (publicPost), the traversal to billingInfo is unauthorized but succeeds because the resolver trusts the parent context.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "GraphQL has no built-in authorization model.",
          "expanded": false
        },
        {
          "id": "n0da20bd0ab5542f",
          "title": "Schema Directives",
          "markdown": "Custom directives annotate the schema with authorization requirements, making the policy visible and declarative:\n\n```graphql\ndirective @auth(requires: Role!) on FIELD_DEFINITION | OBJECT\n\ntype User {\n  id: ID!\n  name: String!\n  email: String! @auth(requires: SELF_OR_ADMIN)\n  ssn: String @auth(requires: ADMIN)\n}\n\ntype BillingInfo @auth(requires: BILLING_ADMIN) {\n  creditCardLast4: String\n  invoices: [Invoice!]!\n}\n\ntype Mutation {\n  deleteUser(id: ID!): Boolean! @auth(requires: ADMIN)\n  updateOrgSettings(input: OrgInput!): Organization! @auth(requires: ORG_ADMIN)\n}\n```\n\nDirectives are resolved at the schema level and can be implemented as field middleware that wraps the original resolver. The advantage is that authorization rules are co-located with the schema definition and visible in schema reviews.",
          "notes": "Custom directives annotate the schema with authorization requirements, making the policy visible and declarative:\n\ndirective @auth(requires: Role!) on FIELDDEFINITION | OBJECT\n\ntype User {\n  id: ID!\n  name: String!\n  email: String! @auth(requires: SELFORADMIN)\n  ssn: String @auth(requires: ADMIN)\n}\n\ntype BillingInfo @auth(requires: BILLINGADMIN) {\n  creditCardLast4: String\n  invoices: [Invoice!]!\n}\n\ntype Mutation {\n  deleteUser(id: ID!): Boolean! @auth(requires: ADMIN)\n  updateOrgSettings(input: OrgInput!): Organization! @auth(requires: ORGADMIN)\n}\n\nDirectives are resolved at the schema level and can be implemented as field middleware that wraps the original resolver. The advantage is that authorization rules are co-located with the schema definition and visible in schema reviews.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Custom directives annotate the schema with authorization requirements,...",
          "expanded": false
        },
        {
          "id": "n9e26a02536ad560",
          "title": "Middleware and Resolver-Level Checks",
          "markdown": "**Middleware approach (graphql-shield):**\n\n`graphql-shield` provides a rules engine that maps authorization logic to the type graph:\n\n```javascript\nimport { shield, rule, and, or } from 'graphql-shield';\n\nconst isAuthenticated = rule()((parent, args, ctx) => ctx.user !== null);\nconst isAdmin = rule()((parent, args, ctx) => ctx.user?.role === 'ADMIN');\nconst isOwner = rule()((parent, args, ctx, info) => ctx.user?.id === parent.id);\n\nconst permissions = shield({\n  Query: {\n    user: isAuthenticated,\n    users: and(isAuthenticated, isAdmin),\n  },\n  User: {\n    email: or(isOwner, isAdmin),\n    ssn: isAdmin,\n  },\n  Mutation: {\n    deleteUser: isAdmin,\n    updateUser: or(isOwner, isAdmin),\n  },\n}, {\n  fallbackRule: isAuthenticated,\n  allowExternalErrors: false,\n});\n```\n\nThe `shield` middleware intercepts resolver execution and evaluates rules before the resolver runs. The `fallbackRule` option provides deny-by-default behavior.\n\n**Resolver-level checks:**\n\nFor fine-grained, instance-specific authorization (e.g., \"can this user access this specific resource?\"), checks must happen inside the resolver with access to the resolved data:\n\n```javascript\nconst resolvers = {\n  Query: {\n    order: async (parent, { id }, context) => {\n      const order = await context.dataSources.orderAPI.getOrder(id);\n      if (order.userId !== context.user.id && context.user.role !== 'ADMIN') {\n        throw new ForbiddenError('Not authorized to view this order');\n      }\n      return order;\n    },\n  },\n};\n```",
          "notes": "Middleware approach (graphql-shield):\n\ngraphql-shield provides a rules engine that maps authorization logic to the type graph:\n\nimport { shield, rule, and, or } from 'graphql-shield';\n\nconst isAuthenticated = rule()((parent, args, ctx) => ctx.user !== null);\nconst isAdmin = rule()((parent, args, ctx) => ctx.user?.role === 'ADMIN');\nconst isOwner = rule()((parent, args, ctx, info) => ctx.user?.id === parent.id);\n\nconst permissions = shield({\n  Query: {\n    user: isAuthenticated,\n    users: and(isAuthenticated, isAdmin),\n  },\n  User: {\n    email: or(isOwner, isAdmin),\n    ssn: isAdmin,\n  },\n  Mutation: {\n    deleteUser: isAdmin,\n    updateUser: or(isOwner, isAdmin),\n  },\n}, {\n  fallbackRule: isAuthenticated,\n  allowExternalErrors: false,\n});\n\nThe shield middleware intercepts resolver execution and evaluates rules before the resolver runs. The fallbackRule option provides deny-by-default behavior.\n\nResolver-level checks:\n\nFor fine-grained, instance-specific authorization (e.g., \"can this user access this specific resource?\"), checks must happen inside the resolver with access to the resolved data:\n\nconst resolvers = {\n  Query: {\n    order: async (parent, { id }, context) => {\n      const order = await context.dataSources.orderAPI.getOrder(id);\n      if (order.userId !== context.user.id && context.user.role !== 'ADMIN') {\n        throw new ForbiddenError('Not authorized to view this order');\n      }\n      return order;\n    },\n  },\n};",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Middleware approach (graphql-shield): graphql-shield provides a rules engine...",
          "expanded": false
        },
        {
          "id": "nb3481d07bb9492a",
          "title": "Policy Engines (OPA, Cedar)",
          "markdown": "For complex authorization requirements, external policy engines provide centralized, auditable policy management:\n\n- **Open Policy Agent (OPA):** Policies written in Rego, evaluated against a JSON input (user, resource, action). The GraphQL middleware sends an authorization query to OPA before each resolver executes.\n- **AWS Cedar:** A policy language designed for fine-grained authorization with built-in support for hierarchical resources, which maps well to GraphQL's type graph.\n\nThe advantage of external policy engines is separation of authorization logic from business logic, centralized policy management, audit logging, and the ability to test policies independently.",
          "notes": "For complex authorization requirements, external policy engines provide centralized, auditable policy management:\n- Open Policy Agent (OPA): Policies written in Rego, evaluated against a JSON input (user, resource, action). The GraphQL middleware sends an authorization query to OPA before each resolver executes.\n- AWS Cedar: A policy language designed for fine-grained authorization with built-in support for hierarchical resources, which maps well to GraphQL's type graph.\n\nThe advantage of external policy engines is separation of authorization logic from business logic, centralized policy management, audit logging, and the ability to test policies independently.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "For complex authorization requirements, external policy engines provide...",
          "expanded": false
        },
        {
          "id": "n1f4cb258de1a649",
          "title": "Deny-by-Default",
          "markdown": "The safest pattern is to deny access to all fields by default and explicitly grant access. This inverts the common failure mode where new fields are accidentally exposed because authorization was forgotten:\n\n```javascript\nconst permissions = shield({\n  Query: {\n    publicPosts: allow,          // Explicitly public\n    me: isAuthenticated,         // Authenticated users only\n    users: isAdmin,              // Admin only\n  },\n}, {\n  fallbackRule: deny,            // Everything else is denied\n});\n```\n\nWhen a developer adds a new field or type, it is inaccessible until an authorization rule is explicitly added. This creates a forcing function for security review.",
          "notes": "The safest pattern is to deny access to all fields by default and explicitly grant access. This inverts the common failure mode where new fields are accidentally exposed because authorization was forgotten:\n\nconst permissions = shield({\n  Query: {\n    publicPosts: allow,          // Explicitly public\n    me: isAuthenticated,         // Authenticated users only\n    users: isAdmin,              // Admin only\n  },\n}, {\n  fallbackRule: deny,            // Everything else is denied\n});\n\nWhen a developer adds a new field or type, it is inaccessible until an authorization rule is explicitly added. This creates a forcing function for security review.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "The safest pattern is to deny access to all fields by default and explicitly...",
          "expanded": false
        }
      ],
      "blurb": "Includes The Core Problem",
      "expanded": false
    },
    {
      "id": "na89f0e0ad6cdfb4",
      "title": "BOLA/IDOR in GraphQL",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "nc72e2aed801fdae",
          "title": "Why GraphQL Makes IDOR Worse",
          "markdown": "Broken Object-Level Authorization (BOLA) / Insecure Direct Object Reference (IDOR) is the #1 API security risk (OWASP API Security Top 10). GraphQL amplifies IDOR risk in several ways:\n\n**Global Relay IDs:** The Relay specification encourages globally unique, base64-encoded IDs like `dXNlcjoxMjM=` (decodes to `user:123`). While convenient for client-side caching, they make ID enumeration trivial - decoding reveals the type and sequential numeric ID.\n\n**Predictable IDs:** Even without Relay, many GraphQL APIs use autoincrement database IDs as the `ID` scalar. Querying `user(id: \"1\")`, `user(id: \"2\")`, `user(id: \"3\")` is straightforward enumeration.\n\n**Nested object traversal:** The graph structure allows reaching objects through multiple paths. Even if `user(id: \"456\")` is properly authorized, an attacker might reach the same user through an unprotected path:\n\n```graphql\nquery {\n  organization(id: \"1\") {\n    members {\n      # Returns all users in the org, including user 456\n      email\n      ssn\n    }\n  }\n}\n```\n\n**Node interface:** The common `node(id: ID!)` query pattern accepts any global ID and returns any type. If authorization is not enforced at the type level, `node(id: \"dXNlcjoxMjM=\")` returns user 123 regardless of who is asking:\n\n```graphql\nquery {\n  node(id: \"dXNlcjoxMjM=\") {\n    ... on User { email ssn }\n    ... on Order { total items { name } }\n    ... on BillingInfo { creditCardLast4 }\n  }\n}\n```",
          "notes": "Broken Object-Level Authorization (BOLA) / Insecure Direct Object Reference (IDOR) is the #1 API security risk (OWASP API Security Top 10). GraphQL amplifies IDOR risk in several ways:\n\nGlobal Relay IDs: The Relay specification encourages globally unique, base64-encoded IDs like dXNlcjoxMjM= (decodes to user:123). While convenient for client-side caching, they make ID enumeration trivial - decoding reveals the type and sequential numeric ID.\n\nPredictable IDs: Even without Relay, many GraphQL APIs use autoincrement database IDs as the ID scalar. Querying user(id: \"1\"), user(id: \"2\"), user(id: \"3\") is straightforward enumeration.\n\nNested object traversal: The graph structure allows reaching objects through multiple paths. Even if user(id: \"456\") is properly authorized, an attacker might reach the same user through an unprotected path:\n\nquery {\n  organization(id: \"1\") {\n    members {\n      # Returns all users in the org, including user 456\n      email\n      ssn\n    }\n  }\n}\n\nNode interface: The common node(id: ID!) query pattern accepts any global ID and returns any type. If authorization is not enforced at the type level, node(id: \"dXNlcjoxMjM=\") returns user 123 regardless of who is asking:\n\nquery {\n  node(id: \"dXNlcjoxMjM=\") {\n    ... on User { email ssn }\n    ... on Order { total items { name } }\n    ... on BillingInfo { creditCardLast4 }\n  }\n}",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Broken Object-Level Authorization (BOLA) / Insecure Direct Object Reference...",
          "expanded": false
        },
        {
          "id": "n340fad3d9f97736",
          "title": "Defense Patterns",
          "markdown": "**Authorization at the data-fetching layer:** Enforce ownership/tenant checks where data is loaded, not just at the query entry point. Every resolver that fetches a resource by ID must verify the requesting user has access:\n\n```javascript\nclass UserAPI extends DataSource {\n  async getUser(id, requestingUser) {\n    const user = await this.db.users.findById(id);\n    if (!user) return null;\n    if (user.organizationId !== requestingUser.organizationId && requestingUser.role !== 'SUPER_ADMIN') {\n      throw new ForbiddenError('Access denied');\n    }\n    return user;\n  }\n}\n```\n\n**Use opaque, non-sequential IDs:** UUIDs or cryptographically random identifiers prevent enumeration. Even with Relay-style global IDs, the underlying identifier should be a UUID, not an autoincrement integer.\n\n**Scope all queries by tenant:** Instead of fetching by global ID, scope queries to the authenticated user's tenant:\n\n```javascript\nconst resolvers = {\n  Query: {\n    users: (parent, args, context) => {\n      return context.dataSources.userAPI.getUsersByOrg(context.user.organizationId, args);\n    },\n  },\n};\n```\n\n**Automated IDOR testing:** Write integration tests that authenticate as User A and attempt to access User B's resources through every query path, including nested traversals. Test both direct ID access and indirect access through relationships.",
          "notes": "Authorization at the data-fetching layer: Enforce ownership/tenant checks where data is loaded, not just at the query entry point. Every resolver that fetches a resource by ID must verify the requesting user has access:\n\nclass UserAPI extends DataSource {\n  async getUser(id, requestingUser) {\n    const user = await this.db.users.findById(id);\n    if (!user) return null;\n    if (user.organizationId !== requestingUser.organizationId && requestingUser.role !== 'SUPERADMIN') {\n      throw new ForbiddenError('Access denied');\n    }\n    return user;\n  }\n}\n\nUse opaque, non-sequential IDs: UUIDs or cryptographically random identifiers prevent enumeration. Even with Relay-style global IDs, the underlying identifier should be a UUID, not an autoincrement integer.\n\nScope all queries by tenant: Instead of fetching by global ID, scope queries to the authenticated user's tenant:\n\nconst resolvers = {\n  Query: {\n    users: (parent, args, context) => {\n      return context.dataSources.userAPI.getUsersByOrg(context.user.organizationId, args);\n    },\n  },\n};\n\nAutomated IDOR testing: Write integration tests that authenticate as User A and attempt to access User B's resources through every query path, including nested traversals. Test both direct ID access and indirect access through relationships.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Authorization at the data-fetching layer: Enforce ownership/tenant checks...",
          "expanded": false
        }
      ],
      "blurb": "Includes Why GraphQL Makes IDOR Worse",
      "expanded": false
    },
    {
      "id": "n54ead2c85db60f0",
      "title": "Rate Limiting for GraphQL",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "nb228c7a6ce3771d",
          "title": "Why Per-Request Rate Limiting Fails",
          "markdown": "Traditional API rate limiting counts HTTP requests per IP or per API key. In GraphQL, this is nearly useless because:",
          "notes": "Traditional API rate limiting counts HTTP requests per IP or per API key. In GraphQL, this is nearly useless because:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "n2d6f6df7d56359f",
              "title": "One HTTP request can contain a batch of 100 operations.",
              "markdown": "- One HTTP request can contain a batch of 100 operations.",
              "notes": "One HTTP request can contain a batch of 100 operations.",
              "blurb": "One HTTP request can contain a batch of 100 operations.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n36dc250e08b381d",
              "title": "One operation can use aliases to invoke 500 resolvers.",
              "markdown": "- One operation can use aliases to invoke 500 resolvers.",
              "notes": "One operation can use aliases to invoke 500 resolvers.",
              "blurb": "One operation can use aliases to invoke 500 resolvers.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n30be1732e949816",
              "title": "Two operations with identical HTTP characteristics can differ by 1000\u00d7 in server-side co",
              "markdown": "- Two operations with identical HTTP characteristics can differ by 1000\u00d7 in server-side co",
              "notes": "Two operations with identical HTTP characteristics can differ by 1000\u00d7 in server-side co",
              "blurb": "Two operations with identical HTTP characteristics can differ by 1000\u00d7 in...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n4b6b9de9bc43216",
              "title": "A query requesting { users { name } } and a query requesting { users { orders { items {",
              "markdown": "- A query requesting { users { name } } and a query requesting { users { orders { items {",
              "notes": "A query requesting { users { name } } and a query requesting { users { orders { items {",
              "blurb": "A query requesting { users { name } } and a query requesting { users {...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "Traditional API rate limiting counts HTTP requests per IP or per API key.",
          "expanded": false
        },
        {
          "id": "ne861a9789a916ff",
          "title": "Operation-Aware Rate Limiting",
          "markdown": "Effective GraphQL rate limiting must understand the operation being executed:\n\n**Cost-based rate limiting:** Assign each client a \"cost budget\" per time window. Each query consumes budget proportional to its computed complexity score. A simple `{ me { name } }` might cost 1 point, while a complex nested query might cost 500 points. The client's budget is decremented by the query's cost, not by 1.\n\n```\nRate limit: 10,000 cost points per minute per API key\n\nQuery: { me { name } }           -> cost: 1    -> budget remaining: 9,999\nQuery: { users(first: 100) { orders { items { name } } } } -> cost: 800 -> budget remaining: 9,199\n```\n\n**Operation-name-based limiting:** Rate limit specific operations independently. The `login` mutation gets a strict limit (5 per minute), while `getProfile` gets a generous one (100 per minute). This requires operations to be named, which is a best practice regardless.\n\n**Tenant and user-level quotas:** Apply different rate limits based on the authenticated user's plan tier, organization, or role. Admins performing bulk operations may need higher limits than regular users.\n\n**Field-specific rate limiting:** Certain expensive or sensitive fields (e.g., `search`, `export`, `recommendations`) can have their own rate limits independent of the overall query budget.",
          "notes": "Effective GraphQL rate limiting must understand the operation being executed:\n\nCost-based rate limiting: Assign each client a \"cost budget\" per time window. Each query consumes budget proportional to its computed complexity score. A simple { me { name } } might cost 1 point, while a complex nested query might cost 500 points. The client's budget is decremented by the query's cost, not by 1.\n\nRate limit: 10,000 cost points per minute per API key\n\nQuery: { me { name } }           -> cost: 1    -> budget remaining: 9,999\nQuery: { users(first: 100) { orders { items { name } } } } -> cost: 800 -> budget remaining: 9,199\n\nOperation-name-based limiting: Rate limit specific operations independently. The login mutation gets a strict limit (5 per minute), while getProfile gets a generous one (100 per minute). This requires operations to be named, which is a best practice regardless.\n\nTenant and user-level quotas: Apply different rate limits based on the authenticated user's plan tier, organization, or role. Admins performing bulk operations may need higher limits than regular users.\n\nField-specific rate limiting: Certain expensive or sensitive fields (e.g., search, export, recommendations) can have their own rate limits independent of the overall query budget.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Effective GraphQL rate limiting must understand the operation being...",
          "expanded": false
        },
        {
          "id": "n914d32d4cae3687",
          "title": "Implementation Patterns",
          "markdown": "```javascript\nimport { createComplexityPlugin } from 'graphql-query-complexity';\n\nconst complexityPlugin = createComplexityPlugin({\n  maximumComplexity: 1000,\n  estimators: [\n    fieldExtensionsEstimator(),\n    simpleEstimator({ defaultComplexity: 1 }),\n  ],\n  onComplete: (complexity) => {\n    console.log(`Query complexity: ${complexity}`);\n  },\n  createError: (max, actual) => {\n    return new GraphQLError(\n      `Query too complex: ${actual}. Maximum allowed: ${max}.`\n    );\n  },\n});\n```\n\nFor cost-based rate limiting across requests, use a token bucket or sliding window algorithm keyed on the authenticated user, with the query's complexity score as the cost per request rather than a flat cost of 1.",
          "notes": "import { createComplexityPlugin } from 'graphql-query-complexity';\n\nconst complexityPlugin = createComplexityPlugin({\n  maximumComplexity: 1000,\n  estimators: [\n    fieldExtensionsEstimator(),\n    simpleEstimator({ defaultComplexity: 1 }),\n  ],\n  onComplete: (complexity) => {\n    console.log(Query complexity: ${complexity});\n  },\n  createError: (max, actual) => {\n    return new GraphQLError(\n      Query too complex: ${actual}. Maximum allowed: ${max}.\n    );\n  },\n});\n\nFor cost-based rate limiting across requests, use a token bucket or sliding window algorithm keyed on the authenticated user, with the query's complexity score as the cost per request rather than a flat cost of 1.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "import { createComplexityPlugin } from 'graphql-query-complexity'; const...",
          "expanded": false
        }
      ],
      "blurb": "Includes Why Per-Request Rate Limiting Fails",
      "expanded": false
    },
    {
      "id": "ndadf11e0a55d703",
      "title": "Persisted Queries and Allowlisted Operations",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n54fab88edf1f1ad",
          "title": "What Persisted Queries Are",
          "markdown": "Persisted queries replace arbitrary query strings with pre-registered query hashes or IDs. Instead of sending the full query text, the client sends a hash:\n\n```json\n{\n  \"extensions\": {\n    \"persistedQuery\": {\n      \"version\": 1,\n      \"sha256Hash\": \"ecf4edb46db40b5132295c0291d62fb65d6759a9ced9853c6e5a6cba4ee3\"\n    }\n  },\n  \"variables\": { \"id\": \"123\" }\n}\n```\n\nThe server looks up the hash in a registry and executes the corresponding query. If the hash is not found, the server either rejects the request (strict mode) or asks the client to send the full query for registration (automatic persisted queries / APQ).",
          "notes": "Persisted queries replace arbitrary query strings with pre-registered query hashes or IDs. Instead of sending the full query text, the client sends a hash:\n\n{\n  \"extensions\": {\n    \"persistedQuery\": {\n      \"version\": 1,\n      \"sha256Hash\": \"ecf4edb46db40b5132295c0291d62fb65d6759a9ced9853c6e5a6cba4ee3\"\n    }\n  },\n  \"variables\": { \"id\": \"123\" }\n}\n\nThe server looks up the hash in a registry and executes the corresponding query. If the hash is not found, the server either rejects the request (strict mode) or asks the client to send the full query for registration (automatic persisted queries / APQ).",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Persisted queries replace arbitrary query strings with pre-registered query...",
          "expanded": false
        },
        {
          "id": "n2066ea1eb95ce6e",
          "title": "Security Benefits",
          "markdown": "**Elimination of arbitrary queries:** In strict mode, the server only executes queries that were explicitly registered during the build/deploy process. Attackers cannot craft novel queries for introspection, depth attacks, or alias abuse because the server rejects any query not in the allowlist.\n\n**Query complexity is known at registration time:** Since every allowed query is pre-analyzed, the server knows the maximum cost of any operation before execution. There are no surprises.\n\n**Reduced attack surface:** Persisted queries effectively convert a flexible GraphQL API into a fixed set of operations, similar to REST endpoints but with GraphQL's type-safety and tooling advantages.\n\n**Bandwidth reduction:** Not sending the full query text reduces request size, which is beneficial for mobile clients.",
          "notes": "Elimination of arbitrary queries: In strict mode, the server only executes queries that were explicitly registered during the build/deploy process. Attackers cannot craft novel queries for introspection, depth attacks, or alias abuse because the server rejects any query not in the allowlist.\n\nQuery complexity is known at registration time: Since every allowed query is pre-analyzed, the server knows the maximum cost of any operation before execution. There are no surprises.\n\nReduced attack surface: Persisted queries effectively convert a flexible GraphQL API into a fixed set of operations, similar to REST endpoints but with GraphQL's type-safety and tooling advantages.\n\nBandwidth reduction: Not sending the full query text reduces request size, which is beneficial for mobile clients.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Elimination of arbitrary queries: In strict mode, the server only executes...",
          "expanded": false
        },
        {
          "id": "n51b826490e15d33",
          "title": "Trade-offs",
          "markdown": "**Development velocity:** Developers must register new queries before they can be used. This adds a build step and coordination between frontend and backend teams. Hot-reloading in development requires a different configuration than production.\n\n**Client-server coupling:** The query registry creates a deployment dependency. The server must have the query registered before the client sends it. This requires coordinated deploys or a registration step in the CI/CD pipeline.\n\n**Flexibility loss:** Ad-hoc queries for debugging, admin tools, or internal dashboards require either a separate endpoint without persisted query enforcement or explicit registration of debugging queries.",
          "notes": "Development velocity: Developers must register new queries before they can be used. This adds a build step and coordination between frontend and backend teams. Hot-reloading in development requires a different configuration than production.\n\nClient-server coupling: The query registry creates a deployment dependency. The server must have the query registered before the client sends it. This requires coordinated deploys or a registration step in the CI/CD pipeline.\n\nFlexibility loss: Ad-hoc queries for debugging, admin tools, or internal dashboards require either a separate endpoint without persisted query enforcement or explicit registration of debugging queries.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Development velocity: Developers must register new queries before they can...",
          "expanded": false
        },
        {
          "id": "n270fd28f7a5aa2a",
          "title": "Staged Rollout Strategy",
          "markdown": "Phase 1 - Monitoring: Deploy APQ in logging mode. Record all queries and their hashes. I",
          "notes": "Phase 1 - Monitoring: Deploy APQ in logging mode. Record all queries and their hashes. I",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "n1facb3b573dd43d",
              "title": "Phase 1 - Monitoring: Deploy APQ in logging mode. Record all queries and their hashes. I",
              "markdown": "- Phase 1 - Monitoring: Deploy APQ in logging mode. Record all queries and their hashes. I",
              "notes": "Phase 1 - Monitoring: Deploy APQ in logging mode. Record all queries and their hashes. I",
              "blurb": "Phase 1 - Monitoring: Deploy APQ in logging mode.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n1b3258c940fb180",
              "title": "Phase 2 - Soft enforcement: Enable APQ with automatic registration. New queries are acce",
              "markdown": "- Phase 2 - Soft enforcement: Enable APQ with automatic registration. New queries are acce",
              "notes": "Phase 2 - Soft enforcement: Enable APQ with automatic registration. New queries are acce",
              "blurb": "Phase 2 - Soft enforcement: Enable APQ with automatic registration.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "ne4e31aeaac576c5",
              "title": "Phase 3 - Strict enforcement for external clients: Mobile and public-facing clients must",
              "markdown": "- Phase 3 - Strict enforcement for external clients: Mobile and public-facing clients must",
              "notes": "Phase 3 - Strict enforcement for external clients: Mobile and public-facing clients must",
              "blurb": "Phase 3 - Strict enforcement for external clients: Mobile and public-facing...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nbea7ac4f10de75f",
              "title": "Phase 4 - Full enforcement: All operations must be persisted. The query registry is mana",
              "markdown": "- Phase 4 - Full enforcement: All operations must be persisted. The query registry is mana",
              "notes": "Phase 4 - Full enforcement: All operations must be persisted. The query registry is mana",
              "blurb": "Phase 4 - Full enforcement: All operations must be persisted.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "Phase 1 - Monitoring: Deploy APQ in logging mode.",
          "expanded": false
        }
      ],
      "blurb": "Includes What Persisted Queries Are",
      "expanded": false
    },
    {
      "id": "nd6f0e544828adcd",
      "title": "GraphQL Subscriptions Security",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n6a95c247eaddcce",
          "title": "How Subscriptions Work",
          "markdown": "GraphQL subscriptions use a persistent transport (typically WebSocket via the `graphql-ws` or legacy `subscriptions-transport-ws` protocol) to push real-time updates to clients. The client sends a subscription operation, and the server sends data whenever the subscribed event occurs:\n\n```graphql\nsubscription {\n  orderStatusChanged(orderId: \"123\") {\n    status\n    updatedAt\n    deliveryEstimate\n  }\n}\n```\n\nThe server maintains the subscription in memory, listening for events and executing the subscription resolver whenever relevant data changes.",
          "notes": "GraphQL subscriptions use a persistent transport (typically WebSocket via the graphql-ws or legacy subscriptions-transport-ws protocol) to push real-time updates to clients. The client sends a subscription operation, and the server sends data whenever the subscribed event occurs:\n\nsubscription {\n  orderStatusChanged(orderId: \"123\") {\n    status\n    updatedAt\n    deliveryEstimate\n  }\n}\n\nThe server maintains the subscription in memory, listening for events and executing the subscription resolver whenever relevant data changes.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "GraphQL subscriptions use a persistent transport (typically WebSocket via...",
          "expanded": false
        },
        {
          "id": "nebb0593c65f8bc5",
          "title": "Security Concerns",
          "markdown": "**WebSocket authentication:** The initial WebSocket handshake does not carry HTTP headers in the same way as regular requests. Authentication must be handled during the connection initialization phase:\n\n```javascript\nconst server = new ApolloServer({ ... });\n\nconst wsServer = new WebSocketServer({ server: httpServer, path: '/graphql' });\n\nuseServer({\n  context: async (ctx) => {\n    const token = ctx.connectionParams?.authToken;\n    if (!token) throw new Error('Missing auth token');\n    const user = await verifyToken(token);\n    if (!user) throw new Error('Invalid token');\n    return { user };\n  },\n  onSubscribe: (ctx, msg) => {\n    // Validate the subscription operation\n    // Check authorization for the subscribed resource\n  },\n}, wsServer);\n```\n\n**Token expiration during long-lived connections:** A WebSocket connection may outlive the authentication token's validity. Unlike HTTP requests where each request carries a fresh token, a WebSocket authenticated at connection time may remain open for hours or days. Implement periodic re-authentication or close connections when tokens expire:",
          "notes": "WebSocket authentication: The initial WebSocket handshake does not carry HTTP headers in the same way as regular requests. Authentication must be handled during the connection initialization phase:\n\nconst server = new ApolloServer({ ... });\n\nconst wsServer = new WebSocketServer({ server: httpServer, path: '/graphql' });\n\nuseServer({\n  context: async (ctx) => {\n    const token = ctx.connectionParams?.authToken;\n    if (!token) throw new Error('Missing auth token');\n    const user = await verifyToken(token);\n    if (!user) throw new Error('Invalid token');\n    return { user };\n  },\n  onSubscribe: (ctx, msg) => {\n    // Validate the subscription operation\n    // Check authorization for the subscribed resource\n  },\n}, wsServer);\n\nToken expiration during long-lived connections: A WebSocket connection may outlive the authentication token's validity. Unlike HTTP requests where each request carries a fresh token, a WebSocket authenticated at connection time may remain open for hours or days. Implement periodic re-authentication or close connections when tokens expire:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "n6295996091067b3",
              "title": "Limit the number of concurrent subscriptions per connection and per user.",
              "markdown": "- Limit the number of concurrent subscriptions per connection and per user.",
              "notes": "Limit the number of concurrent subscriptions per connection and per user.",
              "blurb": "Limit the number of concurrent subscriptions per connection and per user.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n2bede341b03c0d1",
              "title": "Limit the total number of WebSocket connections per IP and per authenticated user.",
              "markdown": "- Limit the total number of WebSocket connections per IP and per authenticated user.",
              "notes": "Limit the total number of WebSocket connections per IP and per authenticated user.",
              "blurb": "Limit the total number of WebSocket connections per IP and per authenticated...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n6531a9a3b6f621a",
              "title": "Implement backpressure: if the client cannot consume events fast enough, drop events or",
              "markdown": "- Implement backpressure: if the client cannot consume events fast enough, drop events or",
              "notes": "Implement backpressure: if the client cannot consume events fast enough, drop events or",
              "blurb": "Implement backpressure: if the client cannot consume events fast enough,...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n1f3a79165413cef",
              "title": "Set idle timeouts: close subscriptions that haven't received events within a configurabl",
              "markdown": "- Set idle timeouts: close subscriptions that haven't received events within a configurabl",
              "notes": "Set idle timeouts: close subscriptions that haven't received events within a configurabl",
              "blurb": "Set idle timeouts: close subscriptions that haven't received events within a...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "WebSocket authentication: The initial WebSocket handshake does not carry...",
          "expanded": false
        }
      ],
      "blurb": "Includes How Subscriptions Work",
      "expanded": false
    },
    {
      "id": "nc03dae77e459556",
      "title": "Federation and Subgraph Security",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n8fa58989da8dbf2",
          "title": "What Federation Is",
          "markdown": "GraphQL federation (Apollo Federation, GraphQL Mesh, or schema stitching) composes multiple GraphQL services (subgraphs) into a unified API served by a gateway (router). Each subgraph owns a portion of the schema:\n\n```\nGateway / Router\n\u251c\u2500\u2500 Users subgraph (owns User type)\n\u251c\u2500\u2500 Orders subgraph (owns Order type, extends User with orders field)\n\u251c\u2500\u2500 Billing subgraph (owns BillingInfo, extends Organization)\n\u2514\u2500\u2500 Inventory subgraph (owns Product, extends Order with items)\n```",
          "notes": "GraphQL federation (Apollo Federation, GraphQL Mesh, or schema stitching) composes multiple GraphQL services (subgraphs) into a unified API served by a gateway (router). Each subgraph owns a portion of the schema:\n\nGateway / Router\n\u251c\u2500\u2500 Users subgraph (owns User type)\n\u251c\u2500\u2500 Orders subgraph (owns Order type, extends User with orders field)\n\u251c\u2500\u2500 Billing subgraph (owns BillingInfo, extends Organization)\n\u2514\u2500\u2500 Inventory subgraph (owns Product, extends Order with items)",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "GraphQL federation (Apollo Federation, GraphQL Mesh, or schema stitching)...",
          "expanded": false
        },
        {
          "id": "n1e0581d76c5791d",
          "title": "Authorization Consistency Across Subgraphs",
          "markdown": "The central security challenge in federation is ensuring consistent authorization across independently developed and deployed subgraphs:\n\n**The gateway trust boundary:** The gateway authenticates the request and passes identity context (user, roles, tenant) to subgraphs via headers or context propagation. Each subgraph must independently enforce authorization - the gateway cannot know what authorization each subgraph field requires.\n\n**Cross-subgraph authorization gaps:** When the Orders subgraph extends the User type with an `orders` field, it receives a User reference from the Users subgraph. The Orders subgraph must independently verify that the requesting user has access to the referenced user's orders. It cannot assume that because the Users subgraph returned the user, the requester is authorized to see that user's orders.\n\n```graphql\n# Orders subgraph\nextend type User @key(fields: \"id\") {\n  id: ID! @external\n  orders: [Order!]! @requires(fields: \"id\")\n}\n\n# The orders resolver MUST check authorization\n# It cannot trust that the User reference implies access\n```",
          "notes": "The central security challenge in federation is ensuring consistent authorization across independently developed and deployed subgraphs:\n\nThe gateway trust boundary: The gateway authenticates the request and passes identity context (user, roles, tenant) to subgraphs via headers or context propagation. Each subgraph must independently enforce authorization - the gateway cannot know what authorization each subgraph field requires.\n\nCross-subgraph authorization gaps: When the Orders subgraph extends the User type with an orders field, it receives a User reference from the Users subgraph. The Orders subgraph must independently verify that the requesting user has access to the referenced user's orders. It cannot assume that because the Users subgraph returned the user, the requester is authorized to see that user's orders.\n\nOrders subgraph\nextend type User @key(fields: \"id\") {\n  id: ID! @external\n  orders: [Order!]! @requires(fields: \"id\")\n}\n\nThe orders resolver MUST check authorization\nIt cannot trust that the User reference implies access",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "nd371017a5714713",
              "title": "Fields that override authorization directives from other subgraphs.",
              "markdown": "- Fields that override authorization directives from other subgraphs.",
              "notes": "Fields that override authorization directives from other subgraphs.",
              "blurb": "Fields that override authorization directives from other subgraphs.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "naa2d494b57d8cde",
              "title": "Types that expose relationships bypassing another subgraph's access controls.",
              "markdown": "- Types that expose relationships bypassing another subgraph's access controls.",
              "notes": "Types that expose relationships bypassing another subgraph's access controls.",
              "blurb": "Types that expose relationships bypassing another subgraph's access controls.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n1a79244a8c31dc5",
              "title": "Input types that accept arguments intended for internal use only.",
              "markdown": "- Input types that accept arguments intended for internal use only.",
              "notes": "Input types that accept arguments intended for internal use only.",
              "blurb": "Input types that accept arguments intended for internal use only.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "The central security challenge in federation is ensuring consistent...",
          "expanded": false
        },
        {
          "id": "nd622e9fa4c5f4fb",
          "title": "Mitigation Patterns",
          "markdown": "**Centralized policy service:** All subgraphs call the same authorization service (e.g., OPA) with the same policy definitions. This ensures consistent authorization decisions regardless of which subgraph is resolving.\n\n**Gateway-level authorization for cross-cutting concerns:** The gateway enforces coarse-grained authorization (authentication, tenant isolation, blocked users) before forwarding to subgraphs. Subgraphs enforce fine-grained, domain-specific authorization.\n\n**Schema linting in CI:** Automated checks on schema changes to verify that authorization directives are present on sensitive fields, that new types include appropriate access controls, and that extending types from other subgraphs doesn't introduce authorization gaps.\n\n**End-to-end authorization tests:** Integration tests that send queries traversing multiple subgraphs and verify authorization is enforced at each boundary.",
          "notes": "Centralized policy service: All subgraphs call the same authorization service (e.g., OPA) with the same policy definitions. This ensures consistent authorization decisions regardless of which subgraph is resolving.\n\nGateway-level authorization for cross-cutting concerns: The gateway enforces coarse-grained authorization (authentication, tenant isolation, blocked users) before forwarding to subgraphs. Subgraphs enforce fine-grained, domain-specific authorization.\n\nSchema linting in CI: Automated checks on schema changes to verify that authorization directives are present on sensitive fields, that new types include appropriate access controls, and that extending types from other subgraphs doesn't introduce authorization gaps.\n\nEnd-to-end authorization tests: Integration tests that send queries traversing multiple subgraphs and verify authorization is enforced at each boundary.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Centralized policy service: All subgraphs call the same authorization...",
          "expanded": false
        }
      ],
      "blurb": "Includes What Federation Is",
      "expanded": false
    },
    {
      "id": "ne17cdba33dfa29b",
      "title": "Error Handling and Information Leakage",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "nfd4028825f829b0",
          "title": "How GraphQL Errors Leak Information",
          "markdown": "GraphQL's error response format is rich and structured, which aids debugging but can leak sensitive information in production:\n\n```json\n{\n  \"errors\": [\n    {\n      \"message\": \"Cannot read property 'organizationId' of null\",\n      \"locations\": [{ \"line\": 3, \"column\": 5 }],\n      \"path\": [\"user\", \"organization\"],\n      \"extensions\": {\n        \"code\": \"INTERNAL_SERVER_ERROR\",\n        \"exception\": {\n          \"stacktrace\": [\n            \"TypeError: Cannot read property 'organizationId' of null\",\n            \"    at UserResolver.organization (/app/src/resolvers/user.js:42:15)\",\n            \"    at processTicksAndRejections (internal/process/task_queues.js:95:5)\"\n          ]\n        }\n      }\n    }\n  ]\n}\n```\n\nThis error reveals: the file structure (`/app/src/resolvers/user.js`), the line number (42), the property name (`organizationId`), the framework and Node.js version, and that the user object can be null.",
          "notes": "GraphQL's error response format is rich and structured, which aids debugging but can leak sensitive information in production:\n\n{\n  \"errors\": [\n    {\n      \"message\": \"Cannot read property 'organizationId' of null\",\n      \"locations\": [{ \"line\": 3, \"column\": 5 }],\n      \"path\": [\"user\", \"organization\"],\n      \"extensions\": {\n        \"code\": \"INTERNALSERVERERROR\",\n        \"exception\": {\n          \"stacktrace\": [\n            \"TypeError: Cannot read property 'organizationId' of null\",\n            \"    at UserResolver.organization (/app/src/resolvers/user.js:42:15)\",\n            \"    at processTicksAndRejections (internal/process/taskqueues.js:95:5)\"\n          ]\n        }\n      }\n    }\n  ]\n}\n\nThis error reveals: the file structure (/app/src/resolvers/user.js), the line number (42), the property name (organizationId), the framework and Node.js version, and that the user object can be null.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "GraphQL's error response format is rich and structured, which aids debugging...",
          "expanded": false
        },
        {
          "id": "n1cab8a05dd41939",
          "title": "Information Leakage Vectors",
          "markdown": "**Stack traces:** Internal implementation details including file paths, line numbers, library versions, and database driver information.\n\n**Field suggestions:** `Cannot query field \"emai\" on type \"User\". Did you mean \"email\"?` - this confirms the existence of the `email` field even when introspection is disabled, enabling schema enumeration one field at a time.\n\n**Database errors:** `\"message\": \"ER_NO_SUCH_TABLE: Table 'prod_db.user_secrets' doesn't exist\"` - reveals database names and table structures.\n\n**Validation errors with internal context:** `\"Argument 'role' must be one of: ADMIN, SUPER_ADMIN, BILLING_ADMIN, INTERNAL_SERVICE\"` - reveals internal role values.\n\n**Partial data with errors:** GraphQL returns partial results alongside errors. A query for five fields where one fails returns four successful results and one error. The error for the restricted field might reveal why access was denied, while the four successful results might include data the user shouldn't have seen.",
          "notes": "Stack traces: Internal implementation details including file paths, line numbers, library versions, and database driver information.\n\nField suggestions: Cannot query field \"emai\" on type \"User\". Did you mean \"email\"? - this confirms the existence of the email field even when introspection is disabled, enabling schema enumeration one field at a time.\n\nDatabase errors: \"message\": \"ERNOSUCHTABLE: Table 'proddb.usersecrets' doesn't exist\" - reveals database names and table structures.\n\nValidation errors with internal context: \"Argument 'role' must be one of: ADMIN, SUPERADMIN, BILLINGADMIN, INTERNALSERVICE\" - reveals internal role values.\n\nPartial data with errors: GraphQL returns partial results alongside errors. A query for five fields where one fails returns four successful results and one error. The error for the restricted field might reveal why access was denied, while the four successful results might include data the user shouldn't have seen.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Stack traces: Internal implementation details including file paths, line...",
          "expanded": false
        },
        {
          "id": "n6e2c20933aa183a",
          "title": "Hardening Error Responses",
          "markdown": "**Mask errors in production:**\n\n```javascript\nconst server = new ApolloServer({\n  formatError: (formattedError, error) => {\n    if (process.env.NODE_ENV === 'production') {\n      // Log the full error internally\n      logger.error('GraphQL error', {\n        message: error.message,\n        stack: error.stack,\n        path: formattedError.path,\n      });\n\n      // Return a sanitized error to the client\n      if (formattedError.extensions?.code === 'INTERNAL_SERVER_ERROR') {\n        return {\n          message: 'An unexpected error occurred',\n          extensions: { code: 'INTERNAL_SERVER_ERROR' },\n        };\n      }\n\n      // Allow known error codes through with sanitized messages\n      return {\n        message: formattedError.message,\n        extensions: { code: formattedError.extensions?.code },\n      };\n    }\n    return formattedError;\n  },\n});\n```\n\n**Disable field suggestions:** Suppress the \"Did you mean\" suggestions in production to prevent schema enumeration.\n\n**Use structured error codes:** Define a set of client-facing error codes (`UNAUTHENTICATED`, `FORBIDDEN`, `NOT_FOUND`, `VALIDATION_ERROR`, `RATE_LIMITED`) and map all errors to these codes. Never expose internal error messages.\n\n**Avoid leaking existence through errors:** When a user queries a resource they don't have access to, return `NOT_FOUND` instead of `FORBIDDEN`. Returning `FORBIDDEN` confirms the resource exists; `NOT_FOUND` is ambiguous (it might not exist, or you might not have access).",
          "notes": "Mask errors in production:\n\nconst server = new ApolloServer({\n  formatError: (formattedError, error) => {\n    if (process.env.NODEENV === 'production') {\n      // Log the full error internally\n      logger.error('GraphQL error', {\n        message: error.message,\n        stack: error.stack,\n        path: formattedError.path,\n      });\n\n      // Return a sanitized error to the client\n      if (formattedError.extensions?.code === 'INTERNALSERVERERROR') {\n        return {\n          message: 'An unexpected error occurred',\n          extensions: { code: 'INTERNALSERVERERROR' },\n        };\n      }\n\n      // Allow known error codes through with sanitized messages\n      return {\n        message: formattedError.message,\n        extensions: { code: formattedError.extensions?.code },\n      };\n    }\n    return formattedError;\n  },\n});\n\nDisable field suggestions: Suppress the \"Did you mean\" suggestions in production to prevent schema enumeration.\n\nUse structured error codes: Define a set of client-facing error codes (UNAUTHENTICATED, FORBIDDEN, NOTFOUND, VALIDATIONERROR, RATELIMITED) and map all errors to these codes. Never expose internal error messages.\n\nAvoid leaking existence through errors: When a user queries a resource they don't have access to, return NOTFOUND instead of FORBIDDEN. Returning FORBIDDEN confirms the resource exists; NOTFOUND is ambiguous (it might not exist, or you might not have access).",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Mask errors in production: const server = new ApolloServer({ formatError:...",
          "expanded": false
        }
      ],
      "blurb": "Includes How GraphQL Errors Leak Information",
      "expanded": false
    },
    {
      "id": "na342af80779e8ce",
      "title": "Comparison with REST API Security",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n01188dad4fb7553",
          "title": "Where GraphQL Is Harder to Secure",
          "markdown": "| Concern | REST | GraphQL |\n|---------|------|---------|\n| **Attack surface enumeration** | Fixed set of routes - documented in OpenAPI spec or discoverable through path fuzzing | Dynamic - introspection reveals the entire schema in one query |\n| **DoS via query complexity** | Each endpoint has predictable cost | Client controls query depth, breadth, and field selection - cost varies by orders of magnitude |\n| **Rate limiting** | Per-endpoint, per-method - straightforward | Per-request limits are insufficient - need operation-aware, cost-based limiting |\n| **Authorization granularity** | Per-route or per-controller - coarse but simple | Per-field, per-type, per-object - complex to implement consistently across the graph |\n| **Batching abuse** | Each operation is a separate HTTP request | Multiple operations or aliases in a single request |\n| **Caching** | HTTP caching (ETags, Cache-Control) works naturally with GET requests and URL-based keys | All requests are POST to the same URL - HTTP caching doesn't work without persisted queries or GET-based queries |\n| **WAF rules** | Path-based and method-based rules map naturally | WAF must parse GraphQL query bodies - most WAFs have limited or no GraphQL support |",
          "notes": "| Concern | REST | GraphQL |\n|---------|------|---------|\n| Attack surface enumeration | Fixed set of routes - documented in OpenAPI spec or discoverable through path fuzzing | Dynamic - introspection reveals the entire schema in one query |\n| DoS via query complexity | Each endpoint has predictable cost | Client controls query depth, breadth, and field selection - cost varies by orders of magnitude |\n| Rate limiting | Per-endpoint, per-method - straightforward | Per-request limits are insufficient - need operation-aware, cost-based limiting |\n| Authorization granularity | Per-route or per-controller - coarse but simple | Per-field, per-type, per-object - complex to implement consistently across the graph |\n| Batching abuse | Each operation is a separate HTTP request | Multiple operations or aliases in a single request |\n| Caching | HTTP caching (ETags, Cache-Control) works naturally with GET requests and URL-based keys | All requests are POST to the same URL - HTTP caching doesn't work without persisted queries or GET-based queries |\n| WAF rules | Path-based and method-based rules map naturally | WAF must parse GraphQL query bodies - most WAFs have limited or no GraphQL support |",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "| Concern | REST | GraphQL | |---------|------|---------| | Attack surface...",
          "expanded": false
        },
        {
          "id": "n34bb79f24ba4178",
          "title": "Where GraphQL Is Easier to Secure",
          "markdown": "| Concern | REST | GraphQL |\n|---------|------|---------|\n| **Over-fetching / data exposure** | Endpoints return fixed response shapes - may include fields the client doesn't need, increasing exposure | Client requests only needed fields - smaller response surface (but the full schema is still accessible) |\n| **Type safety** | Input validation must be manually implemented or configured per-endpoint | Schema provides built-in type validation for all inputs - arguments, input objects, enums are checked before resolvers execute |\n| **Schema as security contract** | API surface is implicit in code - security review requires reading route handlers | Schema is an explicit, reviewable, diffable artifact - schema changes are visible in PRs and can be linted |\n| **Consistent error format** | Error formats vary across endpoints and frameworks | Structured error format is standardized by the spec |\n| **Deprecation visibility** | Deprecated endpoints are documented informally | `@deprecated` directive is built into the spec and visible through introspection and tooling |",
          "notes": "| Concern | REST | GraphQL |\n|---------|------|---------|\n| Over-fetching / data exposure | Endpoints return fixed response shapes - may include fields the client doesn't need, increasing exposure | Client requests only needed fields - smaller response surface (but the full schema is still accessible) |\n| Type safety | Input validation must be manually implemented or configured per-endpoint | Schema provides built-in type validation for all inputs - arguments, input objects, enums are checked before resolvers execute |\n| Schema as security contract | API surface is implicit in code - security review requires reading route handlers | Schema is an explicit, reviewable, diffable artifact - schema changes are visible in PRs and can be linted |\n| Consistent error format | Error formats vary across endpoints and frameworks | Structured error format is standardized by the spec |\n| Deprecation visibility | Deprecated endpoints are documented informally | @deprecated directive is built into the spec and visible through introspection and tooling |",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "| Concern | REST | GraphQL | |---------|------|---------| | Over-fetching /...",
          "expanded": false
        },
        {
          "id": "nc20766f91db945f",
          "title": "Security Parity",
          "markdown": "Some security concerns apply equally to both paradigms: authentication, TLS, CORS, CSRF, input validation against injection (SQL, NoSQL, command), SSRF through user-controlled URLs, logging and monitoring, and secrets management. The attack vectors are the same; only the attack surface shape differs.",
          "notes": "Some security concerns apply equally to both paradigms: authentication, TLS, CORS, CSRF, input validation against injection (SQL, NoSQL, command), SSRF through user-controlled URLs, logging and monitoring, and secrets management. The attack vectors are the same; only the attack surface shape differs.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Some security concerns apply equally to both paradigms: authentication, TLS,...",
          "expanded": false
        }
      ],
      "blurb": "Includes Where GraphQL Is Harder to Secure",
      "expanded": false
    },
    {
      "id": "n8c712ceb6ca2a30",
      "title": "Real-World GraphQL Vulnerabilities and Case Studies",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n0ee3f02951dcc8f",
          "title": "GitLab GraphQL Information Disclosure (2020)",
          "markdown": "GitLab's GraphQL API allowed unauthenticated users to query project information, including private project names and descriptions, through the `projects` query. The issue arose because the GraphQL resolver did not enforce the same visibility checks as the REST API. The REST endpoint correctly filtered private projects, but the GraphQL resolver bypassed this logic. This is a canonical example of authorization inconsistency when a GraphQL layer is added alongside existing REST endpoints.",
          "notes": "GitLab's GraphQL API allowed unauthenticated users to query project information, including private project names and descriptions, through the projects query. The issue arose because the GraphQL resolver did not enforce the same visibility checks as the REST API. The REST endpoint correctly filtered private projects, but the GraphQL resolver bypassed this logic. This is a canonical example of authorization inconsistency when a GraphQL layer is added alongside existing REST endpoints.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "GitLab's GraphQL API allowed unauthenticated users to query project...",
          "expanded": false
        },
        {
          "id": "n67d4f5ec14df1a9",
          "title": "Shopify GraphQL IDOR (2021)",
          "markdown": "A researcher discovered that Shopify's GraphQL API allowed merchants to access other merchants' shop data by manipulating the `shop_id` parameter in GraphQL mutations. The authorization check was present on the REST endpoint but missing from the equivalent GraphQL mutation. Shopify paid a $22,500 bounty for the finding. The root cause was that the GraphQL resolver called an internal service without propagating the authenticated merchant's context.",
          "notes": "A researcher discovered that Shopify's GraphQL API allowed merchants to access other merchants' shop data by manipulating the shopid parameter in GraphQL mutations. The authorization check was present on the REST endpoint but missing from the equivalent GraphQL mutation. Shopify paid a $22,500 bounty for the finding. The root cause was that the GraphQL resolver called an internal service without propagating the authenticated merchant's context.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "A researcher discovered that Shopify's GraphQL API allowed merchants to...",
          "expanded": false
        },
        {
          "id": "n20659f74f0916a1",
          "title": "HackerOne GraphQL Disclosure (2019)",
          "markdown": "HackerOne's own GraphQL API had an issue where the `Team` type's `policy` field was accessible to users who should not have had access. The nested resolution of `team -> policy` did not independently verify that the requesting user had access to the team's policy details. This demonstrated the nested authorization problem: the parent resolver authorized access to the team, but the child resolver for the policy field did not re-check authorization.",
          "notes": "HackerOne's own GraphQL API had an issue where the Team type's policy field was accessible to users who should not have had access. The nested resolution of team -> policy did not independently verify that the requesting user had access to the team's policy details. This demonstrated the nested authorization problem: the parent resolver authorized access to the team, but the child resolver for the policy field did not re-check authorization.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "HackerOne's own GraphQL API had an issue where the Team type's policy field...",
          "expanded": false
        },
        {
          "id": "n7754a7bc43b959f",
          "title": "GraphQL Batching for Brute Force",
          "markdown": "Multiple bug bounty reports across various platforms have documented using GraphQL batch queries for credential stuffing and brute-force attacks. By packing thousands of `login` mutations into a single HTTP request (either via array batching or aliases), attackers bypassed per-request rate limits while attempting credentials at high speed. The fix in each case was to implement operation-aware rate limiting that counts resolver invocations, not HTTP requests.",
          "notes": "Multiple bug bounty reports across various platforms have documented using GraphQL batch queries for credential stuffing and brute-force attacks. By packing thousands of login mutations into a single HTTP request (either via array batching or aliases), attackers bypassed per-request rate limits while attempting credentials at high speed. The fix in each case was to implement operation-aware rate limiting that counts resolver invocations, not HTTP requests.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Multiple bug bounty reports across various platforms have documented using...",
          "expanded": false
        },
        {
          "id": "ncea22b8b695ff0e",
          "title": "GraphQL Introspection in Production APIs",
          "markdown": "Security researchers regularly find major APIs with introspection enabled in production. Surveys have found that a significant percentage of public GraphQL APIs expose their full schema through introspection, including APIs for financial services, healthcare platforms, and government systems. The exposed schemas reveal internal types, admin mutations, and deprecated-but-active fields that attackers use for targeted exploitation.",
          "notes": "Security researchers regularly find major APIs with introspection enabled in production. Surveys have found that a significant percentage of public GraphQL APIs expose their full schema through introspection, including APIs for financial services, healthcare platforms, and government systems. The exposed schemas reveal internal types, admin mutations, and deprecated-but-active fields that attackers use for targeted exploitation.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "Security researchers regularly find major APIs with introspection enabled in...",
          "expanded": false
        }
      ],
      "blurb": "Includes GitLab GraphQL Information Disclosure (2020)",
      "expanded": false
    },
    {
      "id": "neb9c1f44f92629f",
      "title": "Tools and Libraries",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "nb1a27985b0eae26",
          "title": "Apollo Server Security Features",
          "markdown": "Apollo Server, the most widely used GraphQL server for Node.js, includes several built-in security features:",
          "notes": "Apollo Server, the most widely used GraphQL server for Node.js, includes several built-in security features:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "na19921f283ae391",
              "title": "Introspection disabled by default in production (when NODE_ENV=production).",
              "markdown": "- Introspection disabled by default in production (when NODE_ENV=production).",
              "notes": "Introspection disabled by default in production (when NODE_ENV=production).",
              "blurb": "Introspection disabled by default in production (when NODEENV=production).",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nd4233c256c37914",
              "title": "CSRF prevention: Requires a Content-Type header or a custom Apollo-Require-Preflight hea",
              "markdown": "- CSRF prevention: Requires a Content-Type header or a custom Apollo-Require-Preflight hea",
              "notes": "CSRF prevention: Requires a Content-Type header or a custom Apollo-Require-Preflight hea",
              "blurb": "CSRF prevention: Requires a Content-Type header or a custom...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n39afe3139640988",
              "title": "Landing page controls: The Apollo Studio sandbox can be disabled or restricted in produc",
              "markdown": "- Landing page controls: The Apollo Studio sandbox can be disabled or restricted in produc",
              "notes": "Landing page controls: The Apollo Studio sandbox can be disabled or restricted in produc",
              "blurb": "Landing page controls: The Apollo Studio sandbox can be disabled or...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n0a600000156ec4f",
              "title": "Plugin system: Lifecycle hooks for request validation, response formatting, and error ma",
              "markdown": "- Plugin system: Lifecycle hooks for request validation, response formatting, and error ma",
              "notes": "Plugin system: Lifecycle hooks for request validation, response formatting, and error ma",
              "blurb": "Plugin system: Lifecycle hooks for request validation, response formatting,...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n46eee5b364fc3a3",
              "title": "Automatic Persisted Queries (APQ): Built-in support for query hashing and caching.",
              "markdown": "- Automatic Persisted Queries (APQ): Built-in support for query hashing and caching.",
              "notes": "Automatic Persisted Queries (APQ): Built-in support for query hashing and caching.",
              "blurb": "Automatic Persisted Queries (APQ): Built-in support for query hashing and...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "Apollo Server, the most widely used GraphQL server for Node.js, includes...",
          "expanded": false
        },
        {
          "id": "nb1cb1a21e19d8da",
          "title": "graphql-shield",
          "markdown": "A permission layer for GraphQL servers that provides a declarative, composable authorization model:\n\n```javascript\nimport { shield, rule, and, or, not, allow, deny } from 'graphql-shield';\n\nconst isAuthenticated = rule({ cache: 'contextual' })(\n  async (parent, args, ctx) => ctx.user !== null\n);\n\nconst isAdmin = rule({ cache: 'contextual' })(\n  async (parent, args, ctx) => ctx.user?.role === 'ADMIN'\n);\n\nconst isOwner = rule({ cache: 'strict' })(\n  async (parent, args, ctx) => parent.userId === ctx.user?.id\n);\n\nconst permissions = shield({\n  Query: { '*': isAuthenticated },\n  Mutation: { '*': isAuthenticated },\n  User: {\n    email: or(isOwner, isAdmin),\n    ssn: isAdmin,\n  },\n}, {\n  fallbackRule: deny,\n  allowExternalErrors: false,\n  debug: process.env.NODE_ENV !== 'production',\n});\n```\n\nCaching modes (`contextual`, `strict`, `no_cache`) control when rule results are reused across fields, which is important for performance in deeply nested queries.",
          "notes": "A permission layer for GraphQL servers that provides a declarative, composable authorization model:\n\nimport { shield, rule, and, or, not, allow, deny } from 'graphql-shield';\n\nconst isAuthenticated = rule({ cache: 'contextual' })(\n  async (parent, args, ctx) => ctx.user !== null\n);\n\nconst isAdmin = rule({ cache: 'contextual' })(\n  async (parent, args, ctx) => ctx.user?.role === 'ADMIN'\n);\n\nconst isOwner = rule({ cache: 'strict' })(\n  async (parent, args, ctx) => parent.userId === ctx.user?.id\n);\n\nconst permissions = shield({\n  Query: { '': isAuthenticated },\n  Mutation: { '': isAuthenticated },\n  User: {\n    email: or(isOwner, isAdmin),\n    ssn: isAdmin,\n  },\n}, {\n  fallbackRule: deny,\n  allowExternalErrors: false,\n  debug: process.env.NODEENV !== 'production',\n});\n\nCaching modes (contextual, strict, nocache) control when rule results are reused across fields, which is important for performance in deeply nested queries.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "A permission layer for GraphQL servers that provides a declarative,...",
          "expanded": false
        },
        {
          "id": "n7194f790f857858",
          "title": "graphql-depth-limit",
          "markdown": "A simple validation rule that rejects queries exceeding a maximum depth:\n\n```javascript\nimport depthLimit from 'graphql-depth-limit';\n\nconst server = new ApolloServer({\n  validationRules: [depthLimit(10, { ignore: ['__schema'] })],\n});\n```\n\nThe `ignore` option allows introspection queries (which are naturally deep) to bypass the depth limit when introspection is enabled.",
          "notes": "A simple validation rule that rejects queries exceeding a maximum depth:\n\nimport depthLimit from 'graphql-depth-limit';\n\nconst server = new ApolloServer({\n  validationRules: [depthLimit(10, { ignore: ['schema'] })],\n});\n\nThe ignore option allows introspection queries (which are naturally deep) to bypass the depth limit when introspection is enabled.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "A simple validation rule that rejects queries exceeding a maximum depth:...",
          "expanded": false
        },
        {
          "id": "n48f1d3cf98904ef",
          "title": "graphql-query-complexity",
          "markdown": "A more sophisticated cost analysis library that supports field-level cost annotations and multiplier-aware complexity calculation:\n\n```javascript\nimport { getComplexity, fieldExtensionsEstimator, simpleEstimator } from 'graphql-query-complexity';\n\nconst complexity = getComplexity({\n  schema,\n  query: parsedQuery,\n  variables,\n  estimators: [\n    fieldExtensionsEstimator(),\n    simpleEstimator({ defaultComplexity: 1 }),\n  ],\n});\n\nif (complexity > MAX_COMPLEXITY) {\n  throw new Error(`Query complexity ${complexity} exceeds maximum ${MAX_COMPLEXITY}`);\n}\n```",
          "notes": "A more sophisticated cost analysis library that supports field-level cost annotations and multiplier-aware complexity calculation:\n\nimport { getComplexity, fieldExtensionsEstimator, simpleEstimator } from 'graphql-query-complexity';\n\nconst complexity = getComplexity({\n  schema,\n  query: parsedQuery,\n  variables,\n  estimators: [\n    fieldExtensionsEstimator(),\n    simpleEstimator({ defaultComplexity: 1 }),\n  ],\n});\n\nif (complexity > MAXCOMPLEXITY) {\n  throw new Error(Query complexity ${complexity} exceeds maximum ${MAXCOMPLEXITY});\n}",
          "kind": "topic",
          "level": 3,
          "children": [],
          "blurb": "A more sophisticated cost analysis library that supports field-level cost...",
          "expanded": false
        },
        {
          "id": "naa0751065b980e9",
          "title": "Additional Tools",
          "markdown": "graphql-armor: A comprehensive security middleware suite providing depth limiting, cost limiting, alias limiting, character limiting, and disabled introspection in a single package.",
          "notes": "graphql-armor: A comprehensive security middleware suite providing depth limiting, cost limiting, alias limiting, character limiting, and disabled introspection in a single package.",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "n331382d1c78575c",
              "title": "graphql-armor: A comprehensive security middleware suite providing depth limiting, cost",
              "markdown": "- graphql-armor: A comprehensive security middleware suite providing depth limiting, cost",
              "notes": "graphql-armor: A comprehensive security middleware suite providing depth limiting, cost",
              "blurb": "graphql-armor: A comprehensive security middleware suite providing depth...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n5e1cd44c8da6d6a",
              "title": "Stellate (formerly GraphCDN): A GraphQL edge caching and rate limiting proxy that sits i",
              "markdown": "- Stellate (formerly GraphCDN): A GraphQL edge caching and rate limiting proxy that sits i",
              "notes": "Stellate (formerly GraphCDN): A GraphQL edge caching and rate limiting proxy that sits i",
              "blurb": "Stellate (formerly GraphCDN): A GraphQL edge caching and rate limiting proxy...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n0c1037586aa4426",
              "title": "InQL (Burp Suite extension): A security testing tool that performs GraphQL introspection",
              "markdown": "- InQL (Burp Suite extension): A security testing tool that performs GraphQL introspection",
              "notes": "InQL (Burp Suite extension): A security testing tool that performs GraphQL introspection",
              "blurb": "InQL (Burp Suite extension): A security testing tool that performs GraphQL...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n0dfdc1c3c9d983f",
              "title": "graphql-cop: A security auditing tool that checks for common GraphQL misconfigurations:",
              "markdown": "- graphql-cop: A security auditing tool that checks for common GraphQL misconfigurations:",
              "notes": "graphql-cop: A security auditing tool that checks for common GraphQL misconfigurations:",
              "blurb": "graphql-cop: A security auditing tool that checks for common GraphQL...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n13611f72e31b1ea",
              "title": "GraphQL Voyager: A schema visualization tool. While intended for developers, it's also u",
              "markdown": "- GraphQL Voyager: A schema visualization tool. While intended for developers, it's also u",
              "notes": "GraphQL Voyager: A schema visualization tool. While intended for developers, it's also u",
              "blurb": "GraphQL Voyager: A schema visualization tool.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "graphql-armor: A comprehensive security middleware suite providing depth...",
          "expanded": false
        }
      ],
      "blurb": "Includes Apollo Server Security Features",
      "expanded": false
    },
    {
      "id": "n795bd6be9cefb4f",
      "title": "Defenses Summary (Secure-by-Default Checklist)",
      "markdown": "Disable introspection in production. Suppress field suggestions. Publish the schema thro",
      "notes": "Disable introspection in production. Suppress field suggestions. Publish the schema thro",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "nc57b5ed64377a56",
          "title": "Disable introspection in production. Suppress field suggestions. Publish the schema thro",
          "markdown": "- Disable introspection in production. Suppress field suggestions. Publish the schema thro",
          "notes": "Disable introspection in production. Suppress field suggestions. Publish the schema thro",
          "blurb": "Disable introspection in production.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n0cc5305f2caac43",
          "title": "Enforce authorization at every resolver. Use deny-by-default with graphql-shield or sche",
          "markdown": "- Enforce authorization at every resolver. Use deny-by-default with graphql-shield or sche",
          "notes": "Enforce authorization at every resolver. Use deny-by-default with graphql-shield or sche",
          "blurb": "Enforce authorization at every resolver.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n2c72810302efa05",
          "title": "Implement depth limits and cost analysis. Combine graphql-depth-limit with graphql-query",
          "markdown": "- Implement depth limits and cost analysis. Combine graphql-depth-limit with graphql-query",
          "notes": "Implement depth limits and cost analysis. Combine graphql-depth-limit with graphql-query",
          "blurb": "Implement depth limits and cost analysis.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n9bee4fcd3d6e3ed",
          "title": "Limit batching and aliases. Cap batch size. Count aliases in complexity scoring. Conside",
          "markdown": "- Limit batching and aliases. Cap batch size. Count aliases in complexity scoring. Conside",
          "notes": "Limit batching and aliases. Cap batch size. Count aliases in complexity scoring. Conside",
          "blurb": "Limit batching and aliases.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "ne2c91c81d9c48dd",
          "title": "Use operation-aware rate limiting. Rate limit by operation name, complexity cost, and au",
          "markdown": "- Use operation-aware rate limiting. Rate limit by operation name, complexity cost, and au",
          "notes": "Use operation-aware rate limiting. Rate limit by operation name, complexity cost, and au",
          "blurb": "Use operation-aware rate limiting.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n96c94fc7e4ff7fe",
          "title": "Deploy persisted queries for public-facing APIs. Start with APQ in monitoring mode and p",
          "markdown": "- Deploy persisted queries for public-facing APIs. Start with APQ in monitoring mode and p",
          "notes": "Deploy persisted queries for public-facing APIs. Start with APQ in monitoring mode and p",
          "blurb": "Deploy persisted queries for public-facing APIs.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n2aa0666947e9fb8",
          "title": "Harden error responses. Mask stack traces, suppress field suggestions, use generic error",
          "markdown": "- Harden error responses. Mask stack traces, suppress field suggestions, use generic error",
          "notes": "Harden error responses. Mask stack traces, suppress field suggestions, use generic error",
          "blurb": "Harden error responses.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "nebcdee6277d821b",
          "title": "Secure subscriptions. Authenticate WebSocket connections. Check authorization on each ev",
          "markdown": "- Secure subscriptions. Authenticate WebSocket connections. Check authorization on each ev",
          "notes": "Secure subscriptions. Authenticate WebSocket connections. Check authorization on each ev",
          "blurb": "Secure subscriptions.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n2bdabdb981a41bd",
          "title": "Enforce consistent authorization in federated graphs. Use a centralized policy service.",
          "markdown": "- Enforce consistent authorization in federated graphs. Use a centralized policy service.",
          "notes": "Enforce consistent authorization in federated graphs. Use a centralized policy service.",
          "blurb": "Enforce consistent authorization in federated graphs.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n9c42831eed4131d",
          "title": "Use opaque, non-sequential IDs. UUIDs prevent enumeration. Scope all queries by tenant.",
          "markdown": "- Use opaque, non-sequential IDs. UUIDs prevent enumeration. Scope all queries by tenant.",
          "notes": "Use opaque, non-sequential IDs. UUIDs prevent enumeration. Scope all queries by tenant.",
          "blurb": "Use opaque, non-sequential IDs.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        }
      ],
      "blurb": "Disable introspection in production.",
      "expanded": false
    },
    {
      "id": "n0a4ff1ab0b46492",
      "title": "Verification",
      "markdown": "Automated IDOR tests: Authenticate as different users/tenants and traverse every query path.",
      "notes": "Automated IDOR tests: Authenticate as different users/tenants and traverse every query path.",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "nf9d8655b47e5b15",
          "title": "Automated IDOR tests: Authenticate as different users/tenants and traverse every query p",
          "markdown": "- Automated IDOR tests: Authenticate as different users/tenants and traverse every query p",
          "notes": "Automated IDOR tests: Authenticate as different users/tenants and traverse every query p",
          "blurb": "Automated IDOR tests: Authenticate as different users/tenants and traverse...",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n0c9b035e29ca574",
          "title": "Load tests with adversarial queries: Construct worst-case queries (maximum depth, maximu",
          "markdown": "- Load tests with adversarial queries: Construct worst-case queries (maximum depth, maximu",
          "notes": "Load tests with adversarial queries: Construct worst-case queries (maximum depth, maximu",
          "blurb": "Load tests with adversarial queries: Construct worst-case queries (maximum...",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n34196532c60caee",
          "title": "Schema review in PRs: Treat schema changes like API surface changes. Review new fields f",
          "markdown": "- Schema review in PRs: Treat schema changes like API surface changes. Review new fields f",
          "notes": "Schema review in PRs: Treat schema changes like API surface changes. Review new fields f",
          "blurb": "Schema review in PRs: Treat schema changes like API surface changes.",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "nd8657efade6bde7",
          "title": "Logging policy: Verify that the gateway logs operation name, complexity score, execution",
          "markdown": "- Logging policy: Verify that the gateway logs operation name, complexity score, execution",
          "notes": "Logging policy: Verify that the gateway logs operation name, complexity score, execution",
          "blurb": "Logging policy: Verify that the gateway logs operation name, complexity...",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        },
        {
          "id": "n68c17511f47bc88",
          "title": "Chaos testing: Simulate resolver dependency failures (database timeout, downstream servi",
          "markdown": "- Chaos testing: Simulate resolver dependency failures (database timeout, downstream servi",
          "notes": "Chaos testing: Simulate resolver dependency failures (database timeout, downstream servi",
          "blurb": "Chaos testing: Simulate resolver dependency failures (database timeout,...",
          "kind": "topic",
          "level": 3,
          "children": [],
          "expanded": false
        }
      ],
      "blurb": "Automated IDOR tests: Authenticate as different users/tenants and traverse...",
      "expanded": false
    },
    {
      "id": "n54ba96a43e92e01",
      "title": "Advanced Production Pitfalls",
      "markdown": "",
      "notes": "",
      "kind": "topic",
      "level": 2,
      "children": [
        {
          "id": "n324fb4fe99658e0",
          "title": "DataLoader and authorization cache bleed",
          "markdown": "DataLoader improves performance but can accidentally mix trust boundaries if cache keys are too coarse:",
          "notes": "DataLoader improves performance but can accidentally mix trust boundaries if cache keys are too coarse:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "na15b89e3a9736c3",
              "title": "Caching by object ID without tenant/user scope can return cross-tenant data.",
              "markdown": "- Caching by object ID without tenant/user scope can return cross-tenant data.",
              "notes": "Caching by object ID without tenant/user scope can return cross-tenant data.",
              "blurb": "Caching by object ID without tenant/user scope can return cross-tenant data.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nca9ea65faf3bcc6",
              "title": "Reusing DataLoader instances across requests can leak previously authorized results.",
              "markdown": "- Reusing DataLoader instances across requests can leak previously authorized results.",
              "notes": "Reusing DataLoader instances across requests can leak previously authorized results.",
              "blurb": "Reusing DataLoader instances across requests can leak previously authorized...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nf749211a3a27cdf",
              "title": "Resolver-level auth checks that happen *after* cache fetch can return sensitive fields b",
              "markdown": "- Resolver-level auth checks that happen *after* cache fetch can return sensitive fields b",
              "notes": "Resolver-level auth checks that happen *after* cache fetch can return sensitive fields b",
              "blurb": "Resolver-level auth checks that happen after cache fetch can return...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "DataLoader improves performance but can accidentally mix trust boundaries if...",
          "expanded": false
        },
        {
          "id": "nb84150c69a0b586",
          "title": "Persisted query registry abuse",
          "markdown": "Persisted queries reduce arbitrary-query risk, but the registry itself becomes security-critical:",
          "notes": "Persisted queries reduce arbitrary-query risk, but the registry itself becomes security-critical:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "nccff55266844f87",
              "title": "Unauthorized registration/update of query hashes can introduce privileged operations.",
              "markdown": "- Unauthorized registration/update of query hashes can introduce privileged operations.",
              "notes": "Unauthorized registration/update of query hashes can introduce privileged operations.",
              "blurb": "Unauthorized registration/update of query hashes can introduce privileged...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nf9ce8c2f980d5b4",
              "title": "APQ fallback that accepts full query on cache miss can re-open arbitrary query execution",
              "markdown": "- APQ fallback that accepts full query on cache miss can re-open arbitrary query execution",
              "notes": "APQ fallback that accepts full query on cache miss can re-open arbitrary query execution",
              "blurb": "APQ fallback that accepts full query on cache miss can re-open arbitrary...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n0eaaea0add23494",
              "title": "Hash-only telemetry is weak for investigations if operation metadata is missing.",
              "markdown": "- Hash-only telemetry is weak for investigations if operation metadata is missing.",
              "notes": "Hash-only telemetry is weak for investigations if operation metadata is missing.",
              "blurb": "Hash-only telemetry is weak for investigations if operation metadata is...",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "Persisted queries reduce arbitrary-query risk, but the registry itself...",
          "expanded": false
        },
        {
          "id": "ncce72b5797396d8",
          "title": "Subscription auth lifecycle gaps",
          "markdown": "WebSocket subscriptions are long-lived and can outlast auth assumptions:",
          "notes": "WebSocket subscriptions are long-lived and can outlast auth assumptions:",
          "kind": "topic",
          "level": 3,
          "children": [
            {
              "id": "na9f72130dd208b9",
              "title": "Token valid at connect time but expired/revoked later.",
              "markdown": "- Token valid at connect time but expired/revoked later.",
              "notes": "Token valid at connect time but expired/revoked later.",
              "blurb": "Token valid at connect time but expired/revoked later.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "n325b7085a484d12",
              "title": "Role changes not reflected for existing subscriptions.",
              "markdown": "- Role changes not reflected for existing subscriptions.",
              "notes": "Role changes not reflected for existing subscriptions.",
              "blurb": "Role changes not reflected for existing subscriptions.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            },
            {
              "id": "nea3052225cad03b",
              "title": "Tenant context swapped in reconnect flows without proper re-auth.",
              "markdown": "- Tenant context swapped in reconnect flows without proper re-auth.",
              "notes": "Tenant context swapped in reconnect flows without proper re-auth.",
              "blurb": "Tenant context swapped in reconnect flows without proper re-auth.",
              "kind": "topic",
              "level": 4,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "WebSocket subscriptions are long-lived and can outlast auth assumptions:",
          "expanded": false
        }
      ],
      "blurb": "Includes DataLoader and authorization cache bleed",
      "expanded": false
    },
    {
      "id": "n94705d1ef934b53",
      "title": "Operational Reality",
      "markdown": "**Complexity scoring requires iteration:** Initial cost annotations will be wrong. Some fields are more expensive than expected (a \"simple\" field that triggers a service call), and some are less expensive (a field that's always cached). Plan for ongoing tuning based on production resolver metrics.\n\n**Persisted queries slow iteration but improve safety:** The development experience with strict persisted queries is worse - every query change requires registration. Use a staged approach: lenient in development, strict in production. Invest in tooling (IDE plugins, CI/CD query extraction) to reduce friction.\n\n**Federation authorization is an organizational problem:** When multiple teams own subgraphs, authorization consistency requires organizational coordination - shared policy definitions, cross-team schema reviews, and integration tests that span subgraphs. Technical solutions (centralized policy engine) must be paired with process solutions (schema governance).\n\n**GraphQL visibility:** Unlike REST APIs where each endpoint is a distinct log entry with a distinct URL, GraphQL operations all hit the same URL. Without operation-name-based logging and monitoring, all traffic looks identical. Mandate named operations and invest in GraphQL-aware observability tooling.",
      "notes": "Complexity scoring requires iteration: Initial cost annotations will be wrong. Some fields are more expensive than expected (a \"simple\" field that triggers a service call), and some are less expensive (a field that's always cached). Plan for ongoing tuning based on production resolver metrics.\n\nPersisted queries slow iteration but improve safety: The development experience with strict persisted queries is worse - every query change requires registration. Use a staged approach: lenient in development, strict in production. Invest in tooling (IDE plugins, CI/CD query extraction) to reduce friction.\n\nFederation authorization is an organizational problem: When multiple teams own subgraphs, authorization consistency requires organizational coordination - shared policy definitions, cross-team schema reviews, and integration tests that span subgraphs. Technical solutions (centralized policy engine) must be paired with process solutions (schema governance).\n\nGraphQL visibility: Unlike REST APIs where each endpoint is a distinct log entry with a distinct URL, GraphQL operations all hit the same URL. Without operation-name-based logging and monitoring, all traffic looks identical. Mandate named operations and invest in GraphQL-aware observability tooling.",
      "kind": "topic",
      "level": 2,
      "children": [],
      "blurb": "Complexity scoring requires iteration: Initial cost annotations will be wrong.",
      "expanded": false
    },
    {
      "id": "nee43ba899f63536",
      "title": "Quick recall",
      "markdown": "",
      "notes": "",
      "expanded": false,
      "kind": "group",
      "children": [
        {
          "id": "nd88cde4e48205ea",
          "title": "Why different from REST",
          "markdown": "One URL, **schema-driven**, **nested** resolvers -> **field-level** authZ and **cost** matter.",
          "notes": "One URL, schema-driven, nested resolvers -> field-level authZ and cost matter.",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "One URL, schema-driven, nested resolvers -> field-level authZ and cost matter.",
          "expanded": false
        },
        {
          "id": "n3fb69c734ad6e58",
          "title": "Top risks",
          "markdown": "| Risk | Mitigation |\n|------|------------|\n| Introspection / schema leak | Disable or gate in prod |\n| Deep queries / aliases | Depth + **complexity** limits, timeouts |\n| N+1 / DB DoS | DataLoader, pagination, cost analysis |\n| Resolver SSRF | No raw user URLs without allowlist |\n| IDOR in nested types | Re-check ownership **per object** |",
          "notes": "| Risk | Mitigation |\n|------|------------|\n| Introspection / schema leak | Disable or gate in prod |\n| Deep queries / aliases | Depth + complexity limits, timeouts |\n| N+1 / DB DoS | DataLoader, pagination, cost analysis |\n| Resolver SSRF | No raw user URLs without allowlist |\n| IDOR in nested types | Re-check ownership per object |",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "| Risk | Mitigation | |------|------------| | Introspection / schema leak |...",
          "expanded": false
        },
        {
          "id": "nbe9ccb5fcb95d57",
          "title": "Rate limiting",
          "markdown": "Per **operation** / **cost**, not only HTTP per IP.",
          "notes": "Per operation / cost, not only HTTP per IP.",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "Per operation / cost, not only HTTP per IP.",
          "expanded": false
        },
        {
          "id": "nd3bf873247a670d",
          "title": "Interview sound bite",
          "markdown": "\"GraphQL isn't insecure-**missing authZ and cost controls** on a flexible API is.\"",
          "notes": "\"GraphQL isn't insecure-missing authZ and cost controls on a flexible API is.\"",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "\"GraphQL isn't insecure-missing authZ and cost controls on a flexible API is.\"",
          "expanded": false
        }
      ],
      "blurb": "Includes Why different from REST"
    },
    {
      "id": "n7074dc530b0f28b",
      "title": "Misconceptions",
      "markdown": "",
      "notes": "",
      "expanded": false,
      "kind": "group",
      "children": [
        {
          "id": "nf962a9988c80188",
          "title": "\u201cGraphQL is secure because it uses HTTPS\u201d",
          "markdown": "**Truth:** TLS protects **transport**. GraphQL-specific risks are **schema abuse**, **resolver logic**, **authZ at field level**, and **resource exhaustion**-HTTPS does not fix those.",
          "notes": "Truth: TLS protects transport. GraphQL-specific risks are schema abuse, resolver logic, authZ at field level, and resource exhaustion-HTTPS does not fix those.",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "Truth: TLS protects transport.",
          "expanded": false
        },
        {
          "id": "n681d1a3f49a0d7e",
          "title": "\u201cWe can use the same WAF rules as REST\u201d",
          "markdown": "**Truth:** A single endpoint and flexible queries need **application-aware** limits (depth/complexity, persisted queries, resolver budgets). Generic path rules often **miss** GraphQL abuse.",
          "notes": "Truth: A single endpoint and flexible queries need application-aware limits (depth/complexity, persisted queries, resolver budgets). Generic path rules often miss GraphQL abuse.",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "Truth: A single endpoint and flexible queries need application-aware limits...",
          "expanded": false
        },
        {
          "id": "ne37ead4c2d57b04",
          "title": "\u201cIntrospection is fine in production for developers\u201d",
          "markdown": "**Truth:** Introspection **exposes attack surface** to everyone who can reach the endpoint. If enabled, **strong auth**, **network controls**, or **separate** dev/stage endpoints are required.",
          "notes": "Truth: Introspection exposes attack surface to everyone who can reach the endpoint. If enabled, strong auth, network controls, or separate dev/stage endpoints are required.",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "Truth: Introspection exposes attack surface to everyone who can reach the...",
          "expanded": false
        },
        {
          "id": "ncca52c3094f65a5",
          "title": "\u201cOur API gateway authenticates the user\u2014so we\u2019re good\u201d",
          "markdown": "**Truth:** **Authentication \u2260 authorization.** GraphQL requires consistent **object-level** checks-especially for nested fields and **batch** operations.",
          "notes": "Truth: Authentication \u2260 authorization. GraphQL requires consistent object-level checks-especially for nested fields and batch operations.",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "Truth: Authentication \u2260 authorization.",
          "expanded": false
        },
        {
          "id": "nb74eb3d899f8328",
          "title": "\u201cRate limiting per IP is enough\u201d",
          "markdown": "**Truth:** Attackers may stay under IP limits while issuing **expensive** queries. You need **query-aware** throttles and **cost** controls.",
          "notes": "Truth: Attackers may stay under IP limits while issuing expensive queries. You need query-aware throttles and cost controls.",
          "kind": "topic",
          "level": 2,
          "children": [],
          "blurb": "Truth: Attackers may stay under IP limits while issuing expensive queries.",
          "expanded": false
        }
      ],
      "blurb": "Includes \u201cGraphQL is secure because it uses HTTPS\u201d"
    },
    {
      "id": "n37e214391cdfc91",
      "title": "Interview prompts",
      "markdown": "",
      "notes": "",
      "expanded": false,
      "kind": "group",
      "children": [
        {
          "id": "n3d4c2723036ef7d",
          "title": "Fundamentals",
          "markdown": "",
          "notes": "",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "nfca56c812a44437",
              "title": "How is securing GraphQL different from securing a REST API?",
              "markdown": "**Answer:** REST uses multiple endpoints with fixed response shapes, so authorization, rate limiting, and WAF rules map naturally to routes and HTTP methods. GraphQL exposes a **single endpoint** where clients control query **depth, field selection, and nesting**. This means authorization must be enforced at **field/object granularity** rather than route level, rate limiting must be **operation-aware** rather than per-request, and WAF rules based on URL paths are ineffective. The schema is a **queryable attack surface** via introspection, and features like **batching and aliases** allow amplification within a single HTTP request. GraphQL's type system does provide built-in input validation that REST lacks, but the overall security model requires more granular controls at the resolver layer rather than the transport layer.",
              "notes": "Answer: REST uses multiple endpoints with fixed response shapes, so authorization, rate limiting, and WAF rules map naturally to routes and HTTP methods. GraphQL exposes a single endpoint where clients control query depth, field selection, and nesting. This means authorization must be enforced at field/object granularity rather than route level, rate limiting must be operation-aware rather than per-request, and WAF rules based on URL paths are ineffective. The schema is a queryable attack surface via introspection, and features like batching and aliases allow amplification within a single HTTP request. GraphQL's type system does provide built-in input validation that REST lacks, but the overall security model requires more granular controls at the resolver layer rather than the transport layer.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: REST uses multiple endpoints with fixed response shapes, so...",
              "expanded": false
            },
            {
              "id": "n8873f69629f27f0",
              "title": "What is GraphQL introspection, and how should it be handled in...",
              "markdown": "**Answer:** Introspection is a built-in GraphQL capability that lets clients query the **`__schema`** and **`__type`** meta-fields to discover all types, fields, arguments, enums, and relationships in the API. For attackers, this is equivalent to receiving complete API documentation in a single request - they can identify sensitive fields, discover admin mutations, and map traversal paths for IDOR attacks. In production, most teams **disable introspection entirely** (Apollo Server does this by default when `NODE_ENV=production`). Alternatives include restricting introspection to **authenticated internal users**, publishing the schema through a **CI/CD pipeline to a schema registry** (Apollo Studio, GraphQL Hive), and critically, **suppressing field suggestions** in error messages, which can leak schema information even when introspection is disabled. The trade-off is developer experience - teams need alternative ways to explore the API without runtime introspection.",
              "notes": "Answer: Introspection is a built-in GraphQL capability that lets clients query the schema and type meta-fields to discover all types, fields, arguments, enums, and relationships in the API. For attackers, this is equivalent to receiving complete API documentation in a single request - they can identify sensitive fields, discover admin mutations, and map traversal paths for IDOR attacks. In production, most teams disable introspection entirely (Apollo Server does this by default when NODEENV=production). Alternatives include restricting introspection to authenticated internal users, publishing the schema through a CI/CD pipeline to a schema registry (Apollo Studio, GraphQL Hive), and critically, suppressing field suggestions in error messages, which can leak schema information even when introspection is disabled. The trade-off is developer experience - teams need alternative ways to explore the API without runtime introspection.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: Introspection is a built-in GraphQL capability that lets clients...",
              "expanded": false
            },
            {
              "id": "nac1fa4841ce11d3",
              "title": "Explain the query depth and complexity problem in GraphQL. How do you...",
              "markdown": "**Answer:** GraphQL's type graph often contains **circular references** (users -> organizations -> members -> organizations -> ...), letting clients construct queries of arbitrary depth. A deeply nested query can trigger an **exponential number** of resolver calls and database queries from a single HTTP request. Defenses are layered: **depth limiting** (`graphql-depth-limit`) rejects queries beyond a maximum nesting level; **cost analysis** (`graphql-query-complexity`) assigns numeric weights to fields with list multipliers so `users(first:100) { orders }` costs 100\u00d7 a single user; **operation-level timeouts** kill execution after a threshold; and **DataLoader** batches and deduplicates the N+1 queries that depth creates. I tune cost thresholds by analyzing production query patterns - setting limits above the **p99 legitimate query cost** with a safety margin. Too tight breaks real clients; too loose allows abuse. These static-analysis defenses are supplemented by runtime guards like **connection pool limits** and **resolver-level timeouts**.",
              "notes": "Answer: GraphQL's type graph often contains circular references (users -> organizations -> members -> organizations -> ...), letting clients construct queries of arbitrary depth. A deeply nested query can trigger an exponential number of resolver calls and database queries from a single HTTP request. Defenses are layered: depth limiting (graphql-depth-limit) rejects queries beyond a maximum nesting level; cost analysis (graphql-query-complexity) assigns numeric weights to fields with list multipliers so users(first:100) { orders } costs 100\u00d7 a single user; operation-level timeouts kill execution after a threshold; and DataLoader batches and deduplicates the N+1 queries that depth creates. I tune cost thresholds by analyzing production query patterns - setting limits above the p99 legitimate query cost with a safety margin. Too tight breaks real clients; too loose allows abuse. These static-analysis defenses are supplemented by runtime guards like connection pool limits and resolver-level timeouts.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: GraphQL's type graph often contains circular references (users ->...",
              "expanded": false
            },
            {
              "id": "n78b90be0a08e7e8",
              "title": "What are the security implications of GraphQL's single-endpoint design...",
              "markdown": "**Answer:** Traditional WAFs and API gateways rely on **URL paths and HTTP methods** to apply security rules - blocking `/admin/*` for non-admins, rate-limiting `POST /login`, or applying different policies per route. GraphQL collapses everything into `POST /graphql`, making these path-based rules **useless**. A WAF cannot distinguish between a harmless `{ me { name } }` query and a destructive `mutation { deleteAllUsers }` without parsing the GraphQL request body. Most WAFs have **limited or no GraphQL awareness**. This means security controls must move **into the GraphQL layer** itself - operation-name-based rate limiting, query analysis middleware, and resolver-level authorization. Some organizations deploy a **GraphQL-aware proxy** (like Stellate or a custom gateway) that parses operations and applies policies before forwarding to the server.",
              "notes": "Answer: Traditional WAFs and API gateways rely on URL paths and HTTP methods to apply security rules - blocking /admin/ for non-admins, rate-limiting POST /login, or applying different policies per route. GraphQL collapses everything into POST /graphql, making these path-based rules useless. A WAF cannot distinguish between a harmless { me { name } } query and a destructive mutation { deleteAllUsers } without parsing the GraphQL request body. Most WAFs have limited or no GraphQL awareness. This means security controls must move into the GraphQL layer itself - operation-name-based rate limiting, query analysis middleware, and resolver-level authorization. Some organizations deploy a GraphQL-aware proxy (like Stellate or a custom gateway) that parses operations and applies policies before forwarding to the server.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: Traditional WAFs and API gateways rely on URL paths and HTTP methods...",
              "expanded": false
            }
          ],
          "blurb": "Includes How is securing GraphQL different from securing a REST API?",
          "expanded": false
        },
        {
          "id": "nc4dd1e064fd61fd",
          "title": "Authorization and IDOR",
          "markdown": "",
          "notes": "",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "n32069dcad2d65a1",
              "title": "Where do GraphQL authorization bugs typically appear, and how do you...",
              "markdown": "**Answer:** The most common failure is in **nested resolvers** - assuming that because the parent object was authorized, the child fields inherit that authorization. For example, a `publicPost` query might resolve correctly, but the `author` field on the post resolves the full `User` object, and the `organization` field on that user resolves billing information the requester should never see. The fix is enforcing authorization at **every resolver** that fetches data, not just the query entry point. I use **deny-by-default** with `graphql-shield` or schema directives: every field is denied unless explicitly granted. For instance-level checks (can this user access *this specific* order?), authorization logic lives in the **data-fetching layer** so the check happens regardless of which query path reaches the data. I also write **integration tests** that authenticate as User A and attempt to access User B's resources through every nested path in the schema, including indirect traversals.",
              "notes": "Answer: The most common failure is in nested resolvers - assuming that because the parent object was authorized, the child fields inherit that authorization. For example, a publicPost query might resolve correctly, but the author field on the post resolves the full User object, and the organization field on that user resolves billing information the requester should never see. The fix is enforcing authorization at every resolver that fetches data, not just the query entry point. I use deny-by-default with graphql-shield or schema directives: every field is denied unless explicitly granted. For instance-level checks (can this user access this specific order?), authorization logic lives in the data-fetching layer so the check happens regardless of which query path reaches the data. I also write integration tests that authenticate as User A and attempt to access User B's resources through every nested path in the schema, including indirect traversals.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: The most common failure is in nested resolvers - assuming that...",
              "expanded": false
            },
            {
              "id": "ne126f18abb08c7a",
              "title": "How does BOLA/IDOR manifest in GraphQL APIs, and why is it...",
              "markdown": "**Answer:** BOLA/IDOR is amplified in GraphQL by three factors. First, the **Relay specification** encourages globally unique, base64-encoded IDs like `dXNlcjoxMjM=` that decode to `user:123`, making enumeration trivial. Second, the **graph structure** allows reaching objects through multiple paths - even if `user(id: \"456\")` is properly protected, the same user might be accessible through `organization(id: \"1\") { members { email ssn } }` via an unprotected resolver. Third, the generic `node(id: ID!)` query pattern, used for Relay caching, accepts **any global ID** and returns any type, so an attacker can query `node(id: \"billingInfo:789\") { ... on BillingInfo { creditCardLast4 } }`. To defend: use **opaque UUIDs** instead of sequential IDs; enforce authorization at the **data-fetching layer** so every load-by-ID checks ownership/tenant; scope all collection queries by the **authenticated user's tenant**; and explicitly disallow type-guessing through the `node` interface by checking the resolved type against the user's permissions.",
              "notes": "Answer: BOLA/IDOR is amplified in GraphQL by three factors. First, the Relay specification encourages globally unique, base64-encoded IDs like dXNlcjoxMjM= that decode to user:123, making enumeration trivial. Second, the graph structure allows reaching objects through multiple paths - even if user(id: \"456\") is properly protected, the same user might be accessible through organization(id: \"1\") { members { email ssn } } via an unprotected resolver. Third, the generic node(id: ID!) query pattern, used for Relay caching, accepts any global ID and returns any type, so an attacker can query node(id: \"billingInfo:789\") { ... on BillingInfo { creditCardLast4 } }. To defend: use opaque UUIDs instead of sequential IDs; enforce authorization at the data-fetching layer so every load-by-ID checks ownership/tenant; scope all collection queries by the authenticated user's tenant; and explicitly disallow type-guessing through the node interface by checking the resolved type against the user's permissions.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: BOLA/IDOR is amplified in GraphQL by three factors.",
              "expanded": false
            },
            {
              "id": "n72f40e2192dc61e",
              "title": "How do you handle the node(id: ID!) query pattern securely in a...",
              "markdown": "**Answer:** The `node` query is a Relay convention that resolves any object by its global ID, which is extremely convenient for client-side caching but creates a **universal access point** for IDOR attacks. The implementation must first decode the global ID to determine the **type and internal ID**, then dispatch to the appropriate type resolver, and critically, the type resolver must perform **full authorization checks** - not just type-check but ownership/tenant/role verification. I implement a `node` resolver that maintains a registry of type-specific loaders, each with its own authorization logic. The loader for `User` checks tenant membership, the loader for `BillingInfo` checks billing admin role, and so on. Any type without a registered authorized loader returns `null`. I also ensure the global ID encoding is **not just base64** of a predictable pattern - adding a type-specific HMAC or using UUIDs as the underlying ID prevents enumeration even if the encoding scheme is known.",
              "notes": "Answer: The node query is a Relay convention that resolves any object by its global ID, which is extremely convenient for client-side caching but creates a universal access point for IDOR attacks. The implementation must first decode the global ID to determine the type and internal ID, then dispatch to the appropriate type resolver, and critically, the type resolver must perform full authorization checks - not just type-check but ownership/tenant/role verification. I implement a node resolver that maintains a registry of type-specific loaders, each with its own authorization logic. The loader for User checks tenant membership, the loader for BillingInfo checks billing admin role, and so on. Any type without a registered authorized loader returns null. I also ensure the global ID encoding is not just base64 of a predictable pattern - adding a type-specific HMAC or using UUIDs as the underlying ID prevents enumeration even if the encoding scheme is known.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: The node query is a Relay convention that resolves any object by its...",
              "expanded": false
            }
          ],
          "blurb": "Includes Where do GraphQL authorization bugs typically appear, and how do you.",
          "expanded": false
        },
        {
          "id": "nc5f915f5074a7d8",
          "title": "Batching, Aliases, and Rate Limiting",
          "markdown": "",
          "notes": "",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "n856da5eb0824dcc",
              "title": "How do aliases and batching bypass traditional rate limiting, and...",
              "markdown": "**Answer:** GraphQL supports two amplification mechanisms within a single HTTP request. **Array batching** sends multiple operations as a JSON array - 1,000 login mutations in one POST. **Aliases** repeat the same field under different names within a single operation: `a1: login(pass:\"x\") a2: login(pass:\"y\") ...` invokes the resolver 500 times. Traditional per-request rate limiting counts one HTTP request regardless. The fix requires **operation-aware rate limiting**: count **resolver invocations** or **operation names**, not HTTP requests. For cost-based limiting, each query's **complexity score** deducts from a per-user budget. A login mutation that appears 500 times via aliases has 500\u00d7 the cost. I also **cap batch size** (e.g., max 5 operations per HTTP request), ensure the complexity estimator **counts aliases** as separate invocations, and for sensitive mutations like `login` or `resetPassword`, enforce **field-specific rate limits** that count invocations across all requests regardless of batching.",
              "notes": "Answer: GraphQL supports two amplification mechanisms within a single HTTP request. Array batching sends multiple operations as a JSON array - 1,000 login mutations in one POST. Aliases repeat the same field under different names within a single operation: a1: login(pass:\"x\") a2: login(pass:\"y\") ... invokes the resolver 500 times. Traditional per-request rate limiting counts one HTTP request regardless. The fix requires operation-aware rate limiting: count resolver invocations or operation names, not HTTP requests. For cost-based limiting, each query's complexity score deducts from a per-user budget. A login mutation that appears 500 times via aliases has 500\u00d7 the cost. I also cap batch size (e.g., max 5 operations per HTTP request), ensure the complexity estimator counts aliases as separate invocations, and for sensitive mutations like login or resetPassword, enforce field-specific rate limits that count invocations across all requests regardless of batching.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: GraphQL supports two amplification mechanisms within a single HTTP...",
              "expanded": false
            },
            {
              "id": "nfb0f7c6236b031c",
              "title": "Design a rate limiting strategy for a public-facing GraphQL API. What...",
              "markdown": "**Answer:** I design rate limiting on **four dimensions** simultaneously. First, **cost-based budgets**: each authenticated user or API key gets a points budget per time window (e.g., 10,000 points/minute), and each query deducts its computed complexity score - a simple `{ me { name } }` costs 1 point while a complex nested query costs hundreds. Second, **operation-specific limits**: sensitive mutations (`login`, `createAccount`, `resetPassword`) get individual rate limits (5/minute for login) regardless of overall budget. Third, **concurrency limits**: maximum simultaneous in-flight queries per user prevents resource exhaustion even when individual queries are within cost limits. Fourth, **IP-level limits** as a final backstop against unauthenticated abuse. The cost budget uses a **sliding window** algorithm keyed on the authenticated user ID. For unauthenticated endpoints, I combine IP-based limiting with **proof-of-work** or CAPTCHA on critical mutations. All rejected requests return a `RATE_LIMITED` error code with `Retry-After` timing.",
              "notes": "Answer: I design rate limiting on four dimensions simultaneously. First, cost-based budgets: each authenticated user or API key gets a points budget per time window (e.g., 10,000 points/minute), and each query deducts its computed complexity score - a simple { me { name } } costs 1 point while a complex nested query costs hundreds. Second, operation-specific limits: sensitive mutations (login, createAccount, resetPassword) get individual rate limits (5/minute for login) regardless of overall budget. Third, concurrency limits: maximum simultaneous in-flight queries per user prevents resource exhaustion even when individual queries are within cost limits. Fourth, IP-level limits as a final backstop against unauthenticated abuse. The cost budget uses a sliding window algorithm keyed on the authenticated user ID. For unauthenticated endpoints, I combine IP-based limiting with proof-of-work or CAPTCHA on critical mutations. All rejected requests return a RATELIMITED error code with Retry-After timing.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: I design rate limiting on four dimensions simultaneously.",
              "expanded": false
            }
          ],
          "blurb": "Includes How do aliases and batching bypass traditional rate limiting, and...",
          "expanded": false
        },
        {
          "id": "ne5dc78ea14015e9",
          "title": "Subscriptions and Real-Time",
          "markdown": "",
          "notes": "",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "n5eb0555356a16f7",
              "title": "What are the unique security challenges of GraphQL subscriptions?",
              "markdown": "**Answer:** Subscriptions create **persistent WebSocket connections** that differ fundamentally from request-response queries. First, **authentication at connection time** is tricky - WebSocket handshakes don't carry HTTP headers the same way, so auth tokens must be passed through `connectionParams` during the protocol initialization, and the server must validate them before accepting the connection. Second, **token expiration**: a WebSocket may outlive its auth token. I implement **periodic re-validation** or set a timer to close the connection when the token expires. Third, **resource exhaustion**: each subscription consumes server memory and event-processing capacity. An attacker opening thousands of subscriptions can exhaust the server. I cap **concurrent subscriptions per connection** (e.g., 10) and **per user** (e.g., 50), with idle timeouts that close unused subscriptions. Fourth, **authorization on each event**: a user's permissions may change during the subscription lifetime. Every event delivery must **re-check authorization**, not just the initial subscription creation. If a user loses access to an order, the `orderStatusChanged` subscription must stop delivering events.",
              "notes": "Answer: Subscriptions create persistent WebSocket connections that differ fundamentally from request-response queries. First, authentication at connection time is tricky - WebSocket handshakes don't carry HTTP headers the same way, so auth tokens must be passed through connectionParams during the protocol initialization, and the server must validate them before accepting the connection. Second, token expiration: a WebSocket may outlive its auth token. I implement periodic re-validation or set a timer to close the connection when the token expires. Third, resource exhaustion: each subscription consumes server memory and event-processing capacity. An attacker opening thousands of subscriptions can exhaust the server. I cap concurrent subscriptions per connection (e.g., 10) and per user (e.g., 50), with idle timeouts that close unused subscriptions. Fourth, authorization on each event: a user's permissions may change during the subscription lifetime. Every event delivery must re-check authorization, not just the initial subscription creation. If a user loses access to an order, the orderStatusChanged subscription must stop delivering events.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: Subscriptions create persistent WebSocket connections that differ...",
              "expanded": false
            },
            {
              "id": "nc7ac94b54fefb60",
              "title": "How do you authenticate and authorize WebSocket connections for...",
              "markdown": "**Answer:** The `graphql-ws` protocol has a **connection initialization phase** where the client sends `connectionParams` containing the auth token. The server's `onConnect` handler validates this token - verifying JWT signature and expiration, resolving the user, and attaching the user context. If validation fails, the server rejects the connection. For **token renewal**, I support two approaches: the client can send a new `connectionParams` message with a refreshed token, or the server can close the connection with a specific close code (e.g., 4401) when the token expires, prompting the client to reconnect with a fresh token. For **authorization on subscription creation**, the `onSubscribe` handler validates that the user has access to the subscribed resource - `orderStatusChanged(orderId: \"123\")` should fail if the user doesn't own order 123. For **per-event authorization**, I use a `withFilter` wrapper that checks the user's current access before delivering each event, handling cases where permissions change mid-subscription.",
              "notes": "Answer: The graphql-ws protocol has a connection initialization phase where the client sends connectionParams containing the auth token. The server's onConnect handler validates this token - verifying JWT signature and expiration, resolving the user, and attaching the user context. If validation fails, the server rejects the connection. For token renewal, I support two approaches: the client can send a new connectionParams message with a refreshed token, or the server can close the connection with a specific close code (e.g., 4401) when the token expires, prompting the client to reconnect with a fresh token. For authorization on subscription creation, the onSubscribe handler validates that the user has access to the subscribed resource - orderStatusChanged(orderId: \"123\") should fail if the user doesn't own order 123. For per-event authorization, I use a withFilter wrapper that checks the user's current access before delivering each event, handling cases where permissions change mid-subscription.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: The graphql-ws protocol has a connection initialization phase where...",
              "expanded": false
            }
          ],
          "blurb": "Includes What are the unique security challenges of GraphQL subscriptions?",
          "expanded": false
        },
        {
          "id": "nee1a1ee3794ece9",
          "title": "Federation and Architecture",
          "markdown": "",
          "notes": "",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "n55f6d4b8dd79756",
              "title": "In a federated GraphQL architecture, how do you ensure consistent...",
              "markdown": "**Answer:** Federation's core challenge is that **each subgraph team independently implements authorization** for their domain, creating potential inconsistency. My approach has four layers. First, **centralized policy service**: all subgraphs call the same authorization engine (e.g., Open Policy Agent) with shared policy definitions, so the \"can user X access resource Y?\" decision is consistent regardless of which subgraph resolves it. Second, **gateway-level coarse authorization**: the router enforces authentication, tenant isolation, and blocked-user checks before any subgraph receives the request. Third, **schema linting in CI**: automated checks verify that every `@key` extension resolver includes authorization checks, that sensitive fields have `@auth` directives, and that new type extensions don't bypass existing access controls. Fourth, **cross-subgraph integration tests**: end-to-end tests that send queries traversing multiple subgraphs and verify that authorization boundaries hold at each traversal point. I treat the composed schema as a **security boundary** that requires the same review rigor as an API surface change.",
              "notes": "Answer: Federation's core challenge is that each subgraph team independently implements authorization for their domain, creating potential inconsistency. My approach has four layers. First, centralized policy service: all subgraphs call the same authorization engine (e.g., Open Policy Agent) with shared policy definitions, so the \"can user X access resource Y?\" decision is consistent regardless of which subgraph resolves it. Second, gateway-level coarse authorization: the router enforces authentication, tenant isolation, and blocked-user checks before any subgraph receives the request. Third, schema linting in CI: automated checks verify that every @key extension resolver includes authorization checks, that sensitive fields have @auth directives, and that new type extensions don't bypass existing access controls. Fourth, cross-subgraph integration tests: end-to-end tests that send queries traversing multiple subgraphs and verify that authorization boundaries hold at each traversal point. I treat the composed schema as a security boundary that requires the same review rigor as an API surface change.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: Federation's core challenge is that each subgraph team independently...",
              "expanded": false
            },
            {
              "id": "nf28eaf604655d5c",
              "title": "What are the schema composition risks in GraphQL federation, and how...",
              "markdown": "**Answer:** When subgraphs are composed into a unified schema, several risks emerge. A subgraph might **extend a type** owned by another team and add fields that expose data the owning team intended to be internal - for example, the Analytics subgraph extending `User` with a `sessionHistory` field that bypasses the Users subgraph's privacy controls. A subgraph might **override authorization directives** - if the Users subgraph marks `email` as `@auth(requires: SELF_OR_ADMIN)`, but the Orders subgraph extends `User` with a resolver that returns email without checking that directive. Input types can **leak internal arguments** - a subgraph accepting a `role` argument intended for internal service-to-service calls that becomes available to external clients through composition. I mitigate these with **schema governance**: mandatory PR reviews for any type extension, automated composition checks using `rover` CLI that flag directive inconsistencies, a shared `@auth` directive library that subgraphs must use, and a **schema changelog** that tracks which team added which fields to shared types.",
              "notes": "Answer: When subgraphs are composed into a unified schema, several risks emerge. A subgraph might extend a type owned by another team and add fields that expose data the owning team intended to be internal - for example, the Analytics subgraph extending User with a sessionHistory field that bypasses the Users subgraph's privacy controls. A subgraph might override authorization directives - if the Users subgraph marks email as @auth(requires: SELFORADMIN), but the Orders subgraph extends User with a resolver that returns email without checking that directive. Input types can leak internal arguments - a subgraph accepting a role argument intended for internal service-to-service calls that becomes available to external clients through composition. I mitigate these with schema governance: mandatory PR reviews for any type extension, automated composition checks using rover CLI that flag directive inconsistencies, a shared @auth directive library that subgraphs must use, and a schema changelog that tracks which team added which fields to shared types.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: When subgraphs are composed into a unified schema, several risks...",
              "expanded": false
            }
          ],
          "blurb": "Includes In a federated GraphQL architecture, how do you ensure consistent...",
          "expanded": false
        },
        {
          "id": "n9f11d90ec81fa4f",
          "title": "Error Handling",
          "markdown": "",
          "notes": "",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "nd00a0a0b9583daf",
              "title": "How do GraphQL error responses create information leakage, and what's...",
              "markdown": "**Answer:** GraphQL's structured error format is rich by design, which becomes a security liability in production. **Stack traces** in error extensions reveal file paths, line numbers, and framework versions. **Field suggestions** (`Did you mean \"email\"?`) leak valid field names even with introspection disabled, enabling schema enumeration one field at a time. **Database errors** surfaced directly reveal table names and schema structure. **Validation errors** listing allowed enum values expose internal roles or states. My hardening strategy: wrap error formatting to **mask all internal errors** behind generic codes (`INTERNAL_SERVER_ERROR`, `FORBIDDEN`, `NOT_FOUND`), log full error details server-side for debugging. **Suppress field suggestions** in production. Return `NOT_FOUND` instead of `FORBIDDEN` for unauthorized resource access to **avoid confirming resource existence**. Use a defined set of **client-facing error codes** that never leak implementation details. Test by deliberately sending malformed queries and invalid field names, verifying the response contains no internal information.",
              "notes": "Answer: GraphQL's structured error format is rich by design, which becomes a security liability in production. Stack traces in error extensions reveal file paths, line numbers, and framework versions. Field suggestions (Did you mean \"email\"?) leak valid field names even with introspection disabled, enabling schema enumeration one field at a time. Database errors surfaced directly reveal table names and schema structure. Validation errors listing allowed enum values expose internal roles or states. My hardening strategy: wrap error formatting to mask all internal errors behind generic codes (INTERNALSERVERERROR, FORBIDDEN, NOTFOUND), log full error details server-side for debugging. Suppress field suggestions in production. Return NOTFOUND instead of FORBIDDEN for unauthorized resource access to avoid confirming resource existence. Use a defined set of client-facing error codes that never leak implementation details. Test by deliberately sending malformed queries and invalid field names, verifying the response contains no internal information.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: GraphQL's structured error format is rich by design, which becomes a...",
              "expanded": false
            }
          ],
          "blurb": "Includes How do GraphQL error responses create information leakage, and what's",
          "expanded": false
        },
        {
          "id": "nd849ed1d80d5d82",
          "title": "Tooling and Implementation",
          "markdown": "",
          "notes": "",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "nc9b2634c983782f",
              "title": "Walk me through how you'd use graphql-shield to implement...",
              "markdown": "**Answer:** `graphql-shield` wraps every resolver with a permissions layer. I define **atomic rules** - `isAuthenticated`, `isAdmin`, `isOwner` - each returning a boolean from the context. Then I compose them using logical operators: `or(isOwner, isAdmin)`. The critical configuration is **`fallbackRule: deny`**, which means any field without an explicit permission rule is inaccessible. This inverts the common failure mode where new fields are accidentally exposed. For performance, I use the `cache: 'contextual'` option on rules that depend only on context (like `isAuthenticated`) so they're evaluated once per request, and `cache: 'strict'` for rules that depend on the parent object (like `isOwner`). I set `allowExternalErrors: false` in production to prevent internal error messages from leaking through the shield. When a developer adds a new type or field, it's automatically denied until they explicitly add a permission rule - creating a **forcing function** for security review during development.",
              "notes": "Answer: graphql-shield wraps every resolver with a permissions layer. I define atomic rules - isAuthenticated, isAdmin, isOwner - each returning a boolean from the context. Then I compose them using logical operators: or(isOwner, isAdmin). The critical configuration is fallbackRule: deny, which means any field without an explicit permission rule is inaccessible. This inverts the common failure mode where new fields are accidentally exposed. For performance, I use the cache: 'contextual' option on rules that depend only on context (like isAuthenticated) so they're evaluated once per request, and cache: 'strict' for rules that depend on the parent object (like isOwner). I set allowExternalErrors: false in production to prevent internal error messages from leaking through the shield. When a developer adds a new type or field, it's automatically denied until they explicitly add a permission rule - creating a forcing function for security review during development.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: graphql-shield wraps every resolver with a permissions layer.",
              "expanded": false
            },
            {
              "id": "n7f073d4c96efebe",
              "title": "Compare graphql-depth-limit and graphql-query-complexity. When do you...",
              "markdown": "**Answer:** `graphql-depth-limit` is a **blunt instrument** - it rejects queries exceeding a nesting depth (e.g., 10 levels), regardless of how many fields are selected at each level. It's trivial to implement and catches the obvious deeply-nested attack, but a wide query at depth 5 selecting 200 fields with aliases can be far more expensive than a narrow query at depth 15. `graphql-query-complexity` is a **fine-grained cost analyzer** that assigns weights per field with multiplier awareness - `users(first: 100) { orders(first: 50) { ... } }` correctly calculates the multiplicative cost. I use **both together**: depth limiting as a fast first check (cheap to compute, catches obvious abuse), and complexity analysis as the primary cost gate. Neither is sufficient alone - they're **static analysis** tools that don't account for actual resolver execution time. I supplement them with **runtime guards**: operation-level timeouts, resolver-level timeouts, connection pool limits, and monitoring of actual resolver latency. The triad of static limits, runtime guards, and monitoring provides defense in depth.",
              "notes": "Answer: graphql-depth-limit is a blunt instrument - it rejects queries exceeding a nesting depth (e.g., 10 levels), regardless of how many fields are selected at each level. It's trivial to implement and catches the obvious deeply-nested attack, but a wide query at depth 5 selecting 200 fields with aliases can be far more expensive than a narrow query at depth 15. graphql-query-complexity is a fine-grained cost analyzer that assigns weights per field with multiplier awareness - users(first: 100) { orders(first: 50) { ... } } correctly calculates the multiplicative cost. I use both together: depth limiting as a fast first check (cheap to compute, catches obvious abuse), and complexity analysis as the primary cost gate. Neither is sufficient alone - they're static analysis tools that don't account for actual resolver execution time. I supplement them with runtime guards: operation-level timeouts, resolver-level timeouts, connection pool limits, and monitoring of actual resolver latency. The triad of static limits, runtime guards, and monitoring provides defense in depth.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: graphql-depth-limit is a blunt instrument - it rejects queries...",
              "expanded": false
            },
            {
              "id": "n6af0ebd5353633a",
              "title": "What is graphql-armor and when would you choose it over assembling...",
              "markdown": "**Answer:** `graphql-armor` is a **comprehensive security middleware suite** that bundles depth limiting, cost limiting, alias limiting, character limiting, disabled introspection, and query token limiting into a single package with sensible defaults. I choose it when standing up a new GraphQL server and wanting **immediate baseline protection** without assembling and configuring five separate libraries. The advantage is consistent configuration, coordinated updates, and reduced dependency management overhead. The trade-off is less granular control - if I need a custom cost estimator that accounts for field-specific database query costs, or if I need different depth limits for different operation types, the individual libraries offer more flexibility. For mature APIs with specific performance and security requirements, I typically use the individual libraries with custom configuration. For new projects, internal tools, or teams without deep GraphQL security expertise, `graphql-armor` provides better coverage faster with less room for misconfiguration.",
              "notes": "Answer: graphql-armor is a comprehensive security middleware suite that bundles depth limiting, cost limiting, alias limiting, character limiting, disabled introspection, and query token limiting into a single package with sensible defaults. I choose it when standing up a new GraphQL server and wanting immediate baseline protection without assembling and configuring five separate libraries. The advantage is consistent configuration, coordinated updates, and reduced dependency management overhead. The trade-off is less granular control - if I need a custom cost estimator that accounts for field-specific database query costs, or if I need different depth limits for different operation types, the individual libraries offer more flexibility. For mature APIs with specific performance and security requirements, I typically use the individual libraries with custom configuration. For new projects, internal tools, or teams without deep GraphQL security expertise, graphql-armor provides better coverage faster with less room for misconfiguration.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: graphql-armor is a comprehensive security middleware suite that...",
              "expanded": false
            }
          ],
          "blurb": "Includes Walk me through how you'd use graphql-shield to implement...",
          "expanded": false
        },
        {
          "id": "n62f23cdf45d7366",
          "title": "Real-World Scenarios",
          "markdown": "",
          "notes": "",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "n39e4ed4eae3e0dc",
              "title": "A security audit reveals that your GraphQL API returns different error...",
              "markdown": "**Answer:** This is an **enumeration vulnerability** - an attacker can probe user IDs and learn which ones exist based on whether they receive `NOT_FOUND` vs `FORBIDDEN`. With sequential IDs, they can enumerate the entire user base; with email-based lookups, they can verify whether specific email addresses have accounts. The fix is to return **the same error for both cases**: `NOT_FOUND` or a generic `The requested resource could not be found`. The resolver should check authorization first, and if the user lacks access (or the resource doesn't exist), return the same ambiguous response. Internally, I log the **actual reason** (not found vs. unauthorized) for debugging and security monitoring. This pattern applies to all resource lookups - orders, organizations, documents. The principle is that **authorization failures should not confirm resource existence**. I also apply this to timing: both cases should take approximately the same time to respond, preventing timing-based enumeration.",
              "notes": "Answer: This is an enumeration vulnerability - an attacker can probe user IDs and learn which ones exist based on whether they receive NOTFOUND vs FORBIDDEN. With sequential IDs, they can enumerate the entire user base; with email-based lookups, they can verify whether specific email addresses have accounts. The fix is to return the same error for both cases: NOTFOUND or a generic The requested resource could not be found. The resolver should check authorization first, and if the user lacks access (or the resource doesn't exist), return the same ambiguous response. Internally, I log the actual reason (not found vs. unauthorized) for debugging and security monitoring. This pattern applies to all resource lookups - orders, organizations, documents. The principle is that authorization failures should not confirm resource existence. I also apply this to timing: both cases should take approximately the same time to respond, preventing timing-based enumeration.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: This is an enumeration vulnerability - an attacker can probe user...",
              "expanded": false
            },
            {
              "id": "n9cd445a50ea741d",
              "title": "Your organization is migrating from REST to GraphQL. What security...",
              "markdown": "**Answer:** The biggest risk is **authorization parity** - the REST endpoints have years of battle-tested authorization logic, and the new GraphQL resolvers need to enforce identical policies. Multiple real-world breaches (GitLab, Shopify) occurred because GraphQL resolvers called internal services without replicating REST authorization checks. My approach: **don't duplicate authorization logic** - have both REST controllers and GraphQL resolvers call the **same authorization service or data access layer** that enforces access controls. Second, run **parallel testing**: for every REST endpoint being replaced, write tests that make equivalent GraphQL queries as different users and verify identical access control behavior. Third, **migrate incrementally** - keep REST endpoints active during transition and compare responses. Fourth, the single-endpoint nature of GraphQL requires new **monitoring and observability** - REST had distinct URLs for each endpoint in logs; GraphQL needs **operation-name-based logging**. Finally, review **rate limiting** - per-endpoint REST limits don't translate to GraphQL. Implement cost-based limits from day one rather than discovering the gap after a batching attack.",
              "notes": "Answer: The biggest risk is authorization parity - the REST endpoints have years of battle-tested authorization logic, and the new GraphQL resolvers need to enforce identical policies. Multiple real-world breaches (GitLab, Shopify) occurred because GraphQL resolvers called internal services without replicating REST authorization checks. My approach: don't duplicate authorization logic - have both REST controllers and GraphQL resolvers call the same authorization service or data access layer that enforces access controls. Second, run parallel testing: for every REST endpoint being replaced, write tests that make equivalent GraphQL queries as different users and verify identical access control behavior. Third, migrate incrementally - keep REST endpoints active during transition and compare responses. Fourth, the single-endpoint nature of GraphQL requires new monitoring and observability - REST had distinct URLs for each endpoint in logs; GraphQL needs operation-name-based logging. Finally, review rate limiting - per-endpoint REST limits don't translate to GraphQL. Implement cost-based limits from day one rather than discovering the gap after a batching attack.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: The biggest risk is authorization parity - the REST endpoints have...",
              "expanded": false
            },
            {
              "id": "n75b817a0030acaf",
              "title": "Describe how you would investigate and respond to a suspected data...",
              "markdown": "**Answer:** My response follows four phases. **Detection:** I look for anomalous query patterns in GraphQL-aware logs - queries with unusually high complexity scores, queries requesting fields outside normal client patterns (e.g., `ssn`, `internalNotes` appearing in production queries), high alias counts, or queries traversing unusual relationship paths. **Containment:** If an active exfiltration is in progress, I can **block the specific operation pattern** at the gateway, revoke the compromised API key or session, or temporarily restrict the affected query paths. For persisted-query APIs, I can remove the offending query from the registry. **Analysis:** I trace the full query execution path - which resolver returned the data, which authorization checks were evaluated (and which were missing), and what data was actually returned. I correlate with authentication logs to identify the actor. **Remediation:** I patch the authorization gap that allowed the exfiltration, add the missing resolver-level checks, update test coverage to prevent regression, and if the gap was structural (e.g., a missing `@auth` directive pattern), I add **schema linting rules** to CI to catch similar gaps across the entire schema. I also review whether the exfiltrated data triggers breach notification obligations.",
              "notes": "Answer: My response follows four phases. Detection: I look for anomalous query patterns in GraphQL-aware logs - queries with unusually high complexity scores, queries requesting fields outside normal client patterns (e.g., ssn, internalNotes appearing in production queries), high alias counts, or queries traversing unusual relationship paths. Containment: If an active exfiltration is in progress, I can block the specific operation pattern at the gateway, revoke the compromised API key or session, or temporarily restrict the affected query paths. For persisted-query APIs, I can remove the offending query from the registry. Analysis: I trace the full query execution path - which resolver returned the data, which authorization checks were evaluated (and which were missing), and what data was actually returned. I correlate with authentication logs to identify the actor. Remediation: I patch the authorization gap that allowed the exfiltration, add the missing resolver-level checks, update test coverage to prevent regression, and if the gap was structural (e.g., a missing @auth directive pattern), I add schema linting rules to CI to catch similar gaps across the entire schema. I also review whether the exfiltrated data triggers breach notification obligations.",
              "kind": "topic",
              "level": 3,
              "children": [],
              "blurb": "Answer: My response follows four phases.",
              "expanded": false
            }
          ],
          "blurb": "Includes A security audit reveals that your GraphQL API returns different erro",
          "expanded": false
        },
        {
          "id": "n2136b87cab849e4",
          "title": "Depth: Interview Follow-ups \u2014 GraphQL and API Security",
          "markdown": "**Authoritative references:** [OWASP API Security Top 10](https://owasp.org/www-project-api-security/); [GraphQL OWASP Cheat Sheet (draft/community)](https://cheatsheetseries.owasp.org/) - search \"GraphQL\" on OWASP cheat sheet index for latest; [CWE-400](https://cwe.mitre.org/data/definitions/400.html) (resource exhaustion); [Apollo documentation](https://www.apollographql.com/docs/); [GraphQL Specification](https://spec.graphql.org/).\n\n**Follow-ups:**",
          "notes": "Authoritative references: OWASP API Security Top 10; GraphQL OWASP Cheat Sheet (draft/community) - search \"GraphQL\" on OWASP cheat sheet index for latest; CWE-400 (resource exhaustion); Apollo documentation; GraphQL Specification.\n\nFollow-ups:",
          "kind": "topic",
          "level": 2,
          "children": [
            {
              "id": "n7c898dd69189ae0",
              "title": "Field-level authZ: How do you prevent BOLA/IDOR when nested resolvers fetch related obje",
              "markdown": "- Field-level authZ: How do you prevent BOLA/IDOR when nested resolvers fetch related obje",
              "notes": "Field-level authZ: How do you prevent BOLA/IDOR when nested resolvers fetch related obje",
              "blurb": "Field-level authZ: How do you prevent BOLA/IDOR when nested resolvers fetch...",
              "kind": "topic",
              "level": 3,
              "children": [],
              "expanded": false
            },
            {
              "id": "nc37867427e6c730",
              "title": "DoS: Depth/complexity limits vs product need for flexible queries - what's your operatio",
              "markdown": "- DoS: Depth/complexity limits vs product need for flexible queries - what's your operatio",
              "notes": "DoS: Depth/complexity limits vs product need for flexible queries - what's your operatio",
              "blurb": "DoS: Depth/complexity limits vs product need for flexible queries - what's...",
              "kind": "topic",
              "level": 3,
              "children": [],
              "expanded": false
            },
            {
              "id": "na2d18ea0b05dcd1",
              "title": "Introspection: Prod policy and developer workflow alternative (schema registry/CI). What",
              "markdown": "- Introspection: Prod policy and developer workflow alternative (schema registry/CI). What",
              "notes": "Introspection: Prod policy and developer workflow alternative (schema registry/CI). What",
              "blurb": "Introspection: Prod policy and developer workflow alternative (schema...",
              "kind": "topic",
              "level": 3,
              "children": [],
              "expanded": false
            },
            {
              "id": "n11bb9dd8f20dce8",
              "title": "Batching: How do you detect and block alias-based brute force? What if the attacker uses",
              "markdown": "- Batching: How do you detect and block alias-based brute force? What if the attacker uses",
              "notes": "Batching: How do you detect and block alias-based brute force? What if the attacker uses",
              "blurb": "Batching: How do you detect and block alias-based brute force?",
              "kind": "topic",
              "level": 3,
              "children": [],
              "expanded": false
            },
            {
              "id": "n472480117dd8ea0",
              "title": "Federation: Where does authZ live in a federated graph - gateway, subgraph, or both? Wha",
              "markdown": "- Federation: Where does authZ live in a federated graph - gateway, subgraph, or both? Wha",
              "notes": "Federation: Where does authZ live in a federated graph - gateway, subgraph, or both? Wha",
              "blurb": "Federation: Where does authZ live in a federated graph - gateway, subgraph,...",
              "kind": "topic",
              "level": 3,
              "children": [],
              "expanded": false
            },
            {
              "id": "n47148c2fc040572",
              "title": "Subscriptions: How do you handle token expiration on long-lived WebSocket connections? E",
              "markdown": "- Subscriptions: How do you handle token expiration on long-lived WebSocket connections? E",
              "notes": "Subscriptions: How do you handle token expiration on long-lived WebSocket connections? E",
              "blurb": "Subscriptions: How do you handle token expiration on long-lived WebSocket...",
              "kind": "topic",
              "level": 3,
              "children": [],
              "expanded": false
            },
            {
              "id": "n341b79e684cb1c9",
              "title": "Migration: REST-to-GraphQL authorization parity - how do you verify you haven't introduc",
              "markdown": "- Migration: REST-to-GraphQL authorization parity - how do you verify you haven't introduc",
              "notes": "Migration: REST-to-GraphQL authorization parity - how do you verify you haven't introduc",
              "blurb": "Migration: REST-to-GraphQL authorization parity - how do you verify you...",
              "kind": "topic",
              "level": 3,
              "children": [],
              "expanded": false
            }
          ],
          "blurb": "Authoritative references: OWASP API Security Top 10; GraphQL OWASP Cheat...",
          "expanded": false
        }
      ],
      "blurb": "Includes Fundamentals"
    }
  ],
  "topicId": "graphql-and-api-security",
  "name": "GraphQL and API Security",
  "structure": "logic-right",
  "blurb": "GraphQL is a query language and runtime for APIs developed internally at..."
}
